---
phase: 06-api-de-schema-com-decorators-tc39
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/model/index.ts
  - src/types/model.ts
  - src/errors/index.ts
  - test/schema/schema-class-or-plain.test.ts
  - test/schema/decorated-vs-plain-parity.test.ts
  - test/schema/per-insert-defaults.test.ts
  - test/model/registry-config.test.ts
autonomous: true
requirements: [DECO-04]
user_setup: []

must_haves:
  truths:
    - "new Model({ schema: ClasseDecorada }) e new Model({ schema: objetoPlano }) são aceitos de forma transparente pelo construtor, produzindo o mesmo validator e a mesma config (DECO-04, D-08)"
    - "@Schema('users') fornece o collectionName default; o config do Model pode omitir collectionName (herda da classe) ou sobrescrever (D-06)"
    - "Um Model construído com classe decorada valida/rejeita documentos contra MongoDB real exatamente como o Model equivalente construído com objeto plano (paridade DECO-03/DECO-04)"
    - "Um inicializador de campo (createdAt = new Date()) é avaliado POR INSERT — a classe é instanciada fresca a cada insert, resolvendo o footgun de timestamp congelado (D-12)"
    - "Precedência de defaults no insert: doc do usuário > documentDefaults do config > inicializadores da classe (D-13)"
    - "Campos declarados sem inicializador (valor undefined) NÃO são injetados no documento — chaves undefined são filtradas antes do merge, evitando serialização BSON Undefined pelo driver"
  artifacts:
    - test/schema/schema-class-or-plain.test.ts
    - test/schema/decorated-vs-plain-parity.test.ts
    - test/schema/per-insert-defaults.test.ts
  key_links:
    - "src/model/index.ts constructor detecta typeof schema === 'function' + marker kMongoatSchemaClass → chama Schema.compile ANTES de schemaValidatorBuilder"
    - "CreateModelProps.schema (src/types/model.ts) passa a aceitar ModelValidationSchema | SchemaClass<T>"
    - "src/model/index.ts insert/insertMany/bulkWrite: camada classDefaults (filtrada de undefined) entra ANTES de cloneDocumentDefaults no merge, preservando a precedência D-13"
    - "branch de re-registro: candidato que declara hooks (props.hooks) falha alto com MODEL_CONFIG_CONFLICT em vez de descartar hooks silenciosamente (WR-04)"
  prohibitions:
    - statement: "Um hook declarado numa re-registração do mesmo collectionName NÃO pode ser descartado silenciosamente (WR-04)"
      status: resolved
      verification: "test/model/registry-config.test.ts: re-registrar com props.hooks presente lança MongoatError (MODEL_CONFIG_CONFLICT), não retorna a instância existente"
    - statement: "Instância de classe decorada NÃO é spreadada ingenuamente no documento — chaves com valor undefined são removidas antes do merge"
      status: resolved
      verification: "test/schema/per-insert-defaults.test.ts: campo required sem inicializador nem valor do usuário falha por 'required', não por bsonType/serialização confusa"
---

## Phase Goal

**As a** desenvolvedor que consome a lib, **I want to** passar uma classe decorada (ou um objeto plano) como `schema` no construtor do Model de forma transparente e inserir documentos validados server-side, **so that** a API de decorators e a API de objetos coexistem como cidadãs de primeira classe sem eu precisar aprender dois caminhos de registro.

> Fatia vertical (Wave 2): fecha o loop ponta-a-ponta do dev — da classe decorada até a validação `$jsonSchema` no MongoDB real.

<objective>
Integrar as classes decoradas ao construtor do `Model`: detectar classe-decorada vs objeto-plano (D-08), resolver o `collectionName` default de `@Schema` (D-06), aplicar inicializadores de campo como defaults por-insert filtrando `undefined` (D-12/D-13 + Pitfall 3), e fechar o WR-04 (hooks não podem ser descartados em silêncio numa re-registração). Ao fim, `new Model({ schema: ClasseDecorada })` valida/rejeita documentos contra MongoDB real identicamente ao equivalente por objeto plano.

Purpose: Esta é a fatia que torna a API de decorators utilizável de verdade — sem ela, `Schema.compile` (06-01) é só introspecção. Entrega DECO-04.
Output: construtor do Model aceitando ambos os schemas, defaults por-insert seguros, WR-04 fechado, e testes de paridade contra Mongo real.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-api-de-schema-com-decorators-tc39/06-CONTEXT.md
@.planning/phases/06-api-de-schema-com-decorators-tc39/06-RESEARCH.md
@.planning/phases/06-api-de-schema-com-decorators-tc39/06-PATTERNS.md

@src/model/index.ts
@src/types/model.ts
@src/errors/index.ts
@.planning/phases/06-api-de-schema-com-decorators-tc39/06-01-SUMMARY.md
</context>

## Artifacts this phase produces (Plano 06-02)

Símbolos e mudanças NOVOS deste plano (excluir de verificação de drift):

- **`src/model/index.ts` (modificado):** detecção classe-decorada vs objeto-plano no constructor (`isDecoratedSchemaClass`, `resolvedSchema`, `resolvedCollectionName`); armazenamento da referência da classe (`this.schemaClass` ou Symbol privado equivalente); função auxiliar `ownDefinedProperties(instance)` (filtra chaves `undefined`); camada `classDefaults` no merge de `insert`/`insertMany`/`bulkWrite`; helper `getDefaultCollectionName(cls)` (lê `kMongoatSchemaClass`); extensão do branch de re-registro para falhar alto quando o candidato declara hooks (WR-04)
- **`src/types/model.ts` (modificado):** `CreateModelProps.schema` passa a aceitar `ModelValidationSchema | SchemaClass<T>`; `collectionName` passa a ser opcional quando a classe fornece o default (D-06)
- **`src/errors/index.ts` (modificado):** documentar `MODEL_CONFIG_CONFLICT` já reusado para o caso de hooks (nenhuma classe nova)
- **Testes novos:** `test/schema/schema-class-or-plain.test.ts`, `test/schema/decorated-vs-plain-parity.test.ts`, `test/schema/per-insert-defaults.test.ts`; extensão de `test/model/registry-config.test.ts` (cobertura de hooks WR-04)

<tasks>

<task type="auto">
  <name>Task 1: Testes de paridade e detecção (RED) — classe decorada vs objeto plano</name>
  <files>test/schema/schema-class-or-plain.test.ts, test/schema/decorated-vs-plain-parity.test.ts, test/schema/per-insert-defaults.test.ts, test/model/registry-config.test.ts</files>
  <read_first>
    - .planning/phases/06-api-de-schema-com-decorators-tc39/06-RESEARCH.md (Pattern 4 detecção; Pattern 5 defaults filtrando undefined; Pitfall 3; Validation Architecture — Phase Requirement → Test Map)
    - .planning/phases/06-api-de-schema-com-decorators-tc39/06-PATTERNS.md (test/schema/*.test.ts analogs: crud-happy-path, registry-config, schema-clone)
    - test/model/crud-happy-path.test.ts (setup Database.connect + setupCollection + afterAll Database.resetRegistry/disconnect contra Mongo real)
    - test/model/registry-config.test.ts (padrão de detecção de config divergente + Database.resetRegistry em beforeEach)
    - src/schema/decorators.ts e src/schema/compile.ts (criados em 06-01 — API @Schema/@Prop/Schema.compile)
    - src/model/index.ts (constructor ~303-417, insert ~738-759 merge de defaults)
  </read_first>
  <action>
    Escrever PRIMEIRO os testes (RED, rodam sob vitest/esbuild que lowera decorators nativamente):

    `test/schema/schema-class-or-plain.test.ts` (unit): declarar uma classe `@Schema('parity_users')` com `@Prop` para cada campo E o objeto plano equivalente; construir `new Model({ schema: ClasseDecorada })` e `new Model({ schema: objetoPlano, collectionName: 'parity_users_plain' })`; assertar que ambos produzem o MESMO `model.validator` (via deep-equal). Assertar D-06: o Model com classe decorada e SEM `collectionName` no config herda `'parity_users'`; um `collectionName` no config sobrescreve o default da classe.

    `test/schema/decorated-vs-plain-parity.test.ts` (integração, Mongo real via testcontainers, padrão de `crud-happy-path.test.ts`): dois models em collections distintas, um por classe decorada, outro por objeto plano equivalente; após `setupCollection`, inserir o MESMO documento válido em ambos (deve passar) e o MESMO documento inválido em ambos (deve ser rejeitado pelo `$jsonSchema` do servidor com o mesmo comportamento).

    `test/schema/per-insert-defaults.test.ts` (integração, Mongo real): classe decorada com um campo inicializado (`createdAt = new Date()`) e um campo `@Prop` required SEM inicializador; assertar (a) dois inserts consecutivos produzem `createdAt` DIFERENTES (fresco por insert, D-12); (b) precedência D-13 (doc do usuário sobrescreve o inicializador da classe; `documentDefaults` do config sobrescreve o inicializador da classe mas não o doc do usuário); (c) inserir um documento sem o campo required-sem-inicializador falha por `required` (não por bsonType/serialização de BSON Undefined — Pitfall 3).

    Estender `test/model/registry-config.test.ts` (WR-04): re-registrar o MESMO `collectionName` com `props.hooks` presente na segunda construção deve lançar `MongoatError`/`MODEL_CONFIG_CONFLICT` em vez de retornar a instância existente descartando o hook.

    Todos os novos testes devem FALHAR neste ponto (a integração de Model ainda não existe) — confirmar o motivo do RED em cada arquivo antes de encerrar a task.
  </action>
  <verify>
    <automated>test -f test/schema/schema-class-or-plain.test.ts && test -f test/schema/decorated-vs-plain-parity.test.ts && test -f test/schema/per-insert-defaults.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Os três arquivos de teste novos existem e são coletados pelo vitest (typecheck exit 0)
    - `test/model/registry-config.test.ts` contém um caso novo que re-registra com `props.hooks` e espera `MongoatError`
    - Rodar os testes agora produz RED com motivo coerente ("Model não aceita classe decorada" / hooks descartados) — registrado no output da task
    - Nenhum dos novos testes depende de esbuild-only para a semântica de compile (rodam sob vitest normalmente)
  </acceptance_criteria>
  <done>Testes de detecção, paridade Mongo-real, defaults por-insert e WR-04 escritos e falhando pelo motivo certo.</done>
</task>

<task type="auto">
  <name>Task 2: Construtor do Model aceita classe decorada (D-08) + collectionName default de @Schema (D-06)</name>
  <files>src/types/model.ts, src/model/index.ts</files>
  <read_first>
    - src/model/index.ts (constructor ~303-417: desestruturação de props, schemaValidatorBuilder, branch de existing/isSameConfig, registerModel)
    - src/types/model.ts (CreateModelProps ~45-65: schema, collectionName; ModelValidationSchema)
    - src/schema/decorators.ts (kMongoatSchemaClass marker gravado por @Schema; Schema.compile de 06-01)
    - src/types/schema.ts (SchemaClass<T> — criado em 06-01)
    - .planning/phases/06-api-de-schema-com-decorators-tc39/06-RESEARCH.md (Pattern 4 — trecho do constructor: isDecoratedSchemaClass, resolvedSchema, resolvedCollectionName)
  </read_first>
  <action>
    Em `src/types/model.ts`: alterar `CreateModelProps.schema` para `ModelValidationSchema | SchemaClass<ModelType>` e tornar `collectionName` opcional (D-06 — a classe pode fornecer o default). Importar `SchemaClass` de `@/types/schema`. Documentar em JSDoc (sem citar IDs internos) que a classe decorada fornece `collectionName` default via `@Schema('...')`.

    Em `src/model/index.ts`, no início do constructor (ANTES de `schemaValidatorBuilder`): computar `isDecoratedSchemaClass = typeof schema === 'function'`; `resolvedSchema = isDecoratedSchemaClass ? Schema.compile(schema) : schema`; `resolvedCollectionName = collectionName ?? (isDecoratedSchemaClass ? getDefaultCollectionName(schema) : undefined)`. Se `resolvedCollectionName` for nullish, lançar `MongoatValidationError('collectionName é obrigatório — forneça no config do Model ou via @Schema("nome")', { code: 'VALIDATION_FAILED' })`. Passar `resolvedSchema` ao `schemaValidatorBuilder` e usar `resolvedCollectionName` em todo o restante do constructor (registro, isSameConfig, this.collectionName). Guardar a referência da classe (`this.schemaClass = isDecoratedSchemaClass ? schema : undefined`, via propriedade/Symbol privado) para uso no insert (Task 3).

    Adicionar o helper module-level `getDefaultCollectionName(cls)`: lê o marker `kMongoatSchemaClass` gravado por `@Schema` e devolve `collectionName`. Importar `Schema` e o marker/API necessários de `@/schema` (barrel do módulo) — sem criar dependência circular problemática (Model já importa de vários módulos internos; o import de `@/schema` é unidirecional, schema não importa Model).

    NÃO alterar `schemaValidatorBuilder` nem `includeAdditionalPropertiesFalse` — eles continuam recebendo só `ModelValidationSchema` e não sabem nada sobre decorators (o compile já entregou o objeto plano equivalente).
  </action>
  <verify>
    <automated>npm test -- test/schema/schema-class-or-plain.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `test/schema/schema-class-or-plain.test.ts` passa: model por classe decorada e por objeto plano têm `validator` deep-equal
    - Model com classe decorada e sem `collectionName` no config usa o default de `@Schema`; config `collectionName` sobrescreve (D-06)
    - `CreateModelProps.schema` aceita `SchemaClass<T>` sem erro de tipo; `collectionName` opcional
    - Construir Model com objeto plano continua funcionando exatamente como antes (nenhuma regressão em `test/model/*`)
    - `npm run typecheck` exit 0
  </acceptance_criteria>
  <done>Constructor detecta e compila classe decorada; collectionName default de @Schema respeitado; API de objetos intacta (DECO-04, D-06, D-08).</done>
</task>

<task type="auto">
  <name>Task 3: Defaults por-insert filtrando undefined (D-12/D-13) + WR-04 hooks fail-loud</name>
  <files>src/model/index.ts, src/errors/index.ts</files>
  <read_first>
    - src/model/index.ts (insert ~738-759, insertMany ~784-842, bulkWrite ~974-1014 — pontos de merge de defaults; cloneDocumentDefaults ~175-196; isSameConfig ~210-244; branch de existing ~350-370)
    - .planning/phases/06-api-de-schema-com-decorators-tc39/06-RESEARCH.md (Pattern 5 ownDefinedProperties + ordem de merge; Pitfall 3 undefined serializado; Pitfall 4 isSameConfig/WR-04)
    - .planning/phases/06-api-de-schema-com-decorators-tc39/06-PATTERNS.md (merge de defaults; MODEL_CONFIG_CONFLICT; disciplina de clone)
    - test/schema/per-insert-defaults.test.ts e test/model/registry-config.test.ts (criados/estendidos na Task 1)
  </read_first>
  <action>
    Adicionar o helper module-level `ownDefinedProperties(instance)`: devolve `Object.fromEntries(Object.entries(instance).filter(([, v]) => v !== undefined))` — remove chaves `undefined` (campos declarados sem inicializador) que o driver `mongodb` serializaria como BSON `Undefined` (Pitfall 3).

    Em `insert`, `insertMany` e `bulkWrite`: quando `this.schemaClass` existe, instanciar a classe FRESCA por insert (`new this.schemaClass()`), passar por `ownDefinedProperties`, e inserir essa camada `classDefaults` no merge ANTES de `cloneDocumentDefaults(this.documentDefaults)`, preservando a precedência D-13 (`{ ...classDefaults, ...cloneDocumentDefaults(documentDefaults), ...documentDoUsuario }`). Em `insertMany`/`bulkWrite`, instanciar a classe fresca POR documento/operação (cada doc precisa do próprio `createdAt`). Reaproveitar `cloneDocumentDefaults` para os defaults do config; a instância da classe já é fresca (não precisa de clone adicional). Hidratação de defaults SÓ no nível raiz (Open Question 1 do RESEARCH — campos aninhados decorados NÃO recebem inicializadores automáticos; documentar no SUMMARY).

    WR-04 (Pitfall 4) no branch de re-registro do constructor: como funções de hook não são comparáveis estruturalmente por `stableStringify`, NÃO tentar comparar hooks por igualdade. Em vez disso: computar `candidateHasHooks` (true quando `props.hooks` declara qualquer `pre`/`post`). Se um Model já existe para o `collectionName` E `candidateHasHooks` é true, lançar `MongoatValidationError(..., { code: 'MODEL_CONFIG_CONFLICT' })` em vez de retornar a instância existente — falhar alto porque um hook re-declarado seria descartado em silêncio. Manter o caminho atual de `isSameConfig` (reuso da instância) apenas quando o candidato NÃO declara hooks. Deixar o flag `candidateHasHooks` extensível: o Plano 06-04 vai também marcá-lo true quando a classe decorada declarar `@Pre`/`@Post`.
  </action>
  <verify>
    <automated>npm test -- test/schema/per-insert-defaults.test.ts test/schema/decorated-vs-plain-parity.test.ts test/model/registry-config.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test/schema/per-insert-defaults.test.ts` passa: dois inserts produzem `createdAt` diferentes (D-12); precedência doc > documentDefaults > classe (D-13); campo required sem inicializador ausente falha por `required`, não por bsonType (Pitfall 3)
    - `test/schema/decorated-vs-plain-parity.test.ts` passa: classe decorada e objeto plano validam/rejeitam identicamente contra Mongo real
    - `test/model/registry-config.test.ts` passa: re-registro com `props.hooks` presente lança `MongoatError`/`MODEL_CONFIG_CONFLICT` (WR-04); re-registro sem hooks e config idêntica continua reusando a instância
    - Helper `ownDefinedProperties` remove chaves `undefined` antes do merge
    - `npm run build` verde (a integração de Model não quebra o build de produção)
  </acceptance_criteria>
  <done>Defaults por-insert frescos e sem chaves undefined (D-12/D-13); WR-04 fecha fail-loud para hooks; build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app do dev → Model.insert → MongoDB | Documento potencialmente derivado de input de usuário final cruza para o driver; a validação `$jsonSchema` server-side é a fronteira de confiança |
| declaração de schema (classe) → validator compilado | Um schema compilado incorreto enfraquece a validação server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02-01 | Repudiation / Tampering | hook de segurança (`@Pre`/`props.hooks`) descartado em silêncio numa re-registração (WR-04) | high | mitigate | branch de re-registro falha alto com `MODEL_CONFIG_CONFLICT` quando o candidato declara hooks; teste em `registry-config.test.ts` |
| T-06-02-02 | Tampering | chaves `undefined` de campos sem inicializador serializadas como BSON `Undefined`, confundindo a validação `$jsonSchema` | medium | mitigate | `ownDefinedProperties` filtra `undefined` antes do merge; teste de integração `per-insert-defaults.test.ts` |
| T-06-02-03 | Tampering / sub-validação | paridade quebrada entre classe decorada e objeto plano geraria validação server-side divergente | medium | mitigate | teste de paridade contra Mongo real (`decorated-vs-plain-parity.test.ts`) |
</threat_model>

<verification>
- `npm test -- test/schema/schema-class-or-plain.test.ts test/schema/decorated-vs-plain-parity.test.ts test/schema/per-insert-defaults.test.ts` verde
- `npm test -- test/model/registry-config.test.ts` verde (WR-04 + regressões existentes)
- `npm run typecheck` exit 0; `npm run build` verde
- Suíte de Model existente (`test/model/*`) sem regressão
</verification>

<success_criteria>
`new Model({ schema: ClasseDecorada })` e `new Model({ schema: objetoPlano })` coexistem transparentemente (DECO-04); collectionName default de `@Schema` respeitado (D-06); defaults por-insert frescos e seguros (D-12/D-13); WR-04 fechado (hooks nunca descartados em silêncio).
</success_criteria>

<output>
Create `.planning/phases/06-api-de-schema-com-decorators-tc39/06-02-SUMMARY.md` when done. Registrar: decisão sobre hidratação de defaults só no nível raiz (Open Question 1) e como o flag `candidateHasHooks` foi deixado extensível para o Plano 06-04.
</output>