---
phase: 05-estabiliza-o-de-api-e-release-v1-0
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .changeset/config.json
  - .changeset/README.md
  - .github/workflows/release.yml
autonomous: true
requirements:
  - REL-01
user_setup:
  - service: npm
    why: "Publicar @iamcalegari/mongoat via GitHub Actions com provenance (D-04)"
    env_vars:
      - name: NPM_TOKEN
        source: "npmjs.com -> Access Tokens -> Generate New Token -> Granular Access Token (escopo minimo: Read and write APENAS para o pacote @iamcalegari/mongoat). Adicionar em GitHub repo -> Settings -> Secrets and variables -> Actions -> New repository secret (nome: NPM_TOKEN)"
    dashboard_config:
      - task: "Criar GitHub Environment 'npm-publish' com Required reviewers = o autor — gate humano da 1a publicacao (D-03: gated na 1a, aprovacao de 1 clique nas seguintes)"
        location: "GitHub repo -> Settings -> Environments -> New environment -> npm-publish -> Required reviewers"
must_haves:
  truths:
    - "`npx changeset status` roda sem erro (changesets inicializado e configurado)"
    - ".changeset/config.json tem changelog:false, access:public e baseBranch:main"
    - "Existe .github/workflows/release.yml SEPARADO de ci.yml/docs.yml, disparando so em push para main"
    - "release.yml declara permissions id-token:write e NPM_CONFIG_PROVENANCE:true e usa environment npm-publish (gate D-03)"
    - "package.json tem um script release que roda changeset publish"
  artifacts:
    - .changeset/config.json
    - .github/workflows/release.yml
    - package.json
  key_links:
    - "release.yml <-> secret NPM_TOKEN + OIDC id-token:write (provenance, D-04)"
    - "changesets/action <-> script release (changeset publish) do package.json"
    - "release.yml <-> GitHub Environment npm-publish (required reviewer, D-03)"
---

<objective>
Montar o pipeline de release com changesets e o workflow de publicacao com provenance, buildando/validando localmente (REL-01, D-03, D-04) — sem publicar nada ainda. O `.changeset/config.json` preserva o CHANGELOG.md mantido a mao (`changelog:false`); o `release.yml` reaproveita o padrao OIDC ja usado em `docs.yml` para atestar proveniencia.

## Phase Goal

**As a** mantenedor do Mongoat, **I want to** ter um pipeline changesets que gera o Version PR e publica no npm com provenance, gated na 1a release, **so that** as releases sejam auditaveis, reprodutiveis e originadas do CI (nunca do laptop).

Purpose: O pipeline e a infraestrutura sobre a qual o RC (05-03) e a estavel (05-05) publicam; precisa existir e validar antes de qualquer publish.
Output: `.changeset/` configurado, `release.yml` separado com provenance + gate, script `release` no package.json.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-estabiliza-o-de-api-e-release-v1-0/05-CONTEXT.md
@.planning/phases/05-estabiliza-o-de-api-e-release-v1-0/05-RESEARCH.md
@package.json
@.github/workflows/ci.yml
@.github/workflows/docs.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar e configurar @changesets/cli (changelog:false, access:public)</name>
  <read_first>
    package.json (scripts + devDependencies + publishConfig),
    05-RESEARCH.md secao Standard Stack (versao exata + legitimidade) e Code Examples (.changeset/config.json recomendado),
    05-RESEARCH.md secao Package Legitimacy Audit (verdict de @changesets/cli)
  </read_first>
  <files>package.json, .changeset/config.json, .changeset/README.md</files>
  <action>
    Instalar `@changesets/cli@2.31.0` como devDependency (versao exata verificada e aprovada no Package Legitimacy Audit do research — verdict OK, NAO e [ASSUMED]/[SUS], portanto sem checkpoint de supply-chain bloqueante). Rodar `npx changeset init` para gerar `.changeset/config.json` + `.changeset/README.md`.
    Editar `.changeset/config.json` conforme o Code Example do research: `changelog` false (preserva o CHANGELOG.md mantido a mao — Pattern 1 / Pitfall 3, evita que `changeset version` sobrescreva a curadoria Keep-a-Changelog), `commit` false, `access` public (reflete o publishConfig existente), `baseBranch` main, `updateInternalDependencies` patch, arrays fixed/linked/ignore vazios.
    Adicionar ao package.json um script `release` = `changeset publish` (comando que o `changesets/action` invocara no job de publish). NAO editar `package.json.version` a mao (o bump e do `changeset version`, nas proximas waves).
  </action>
  <verify>
    <automated>npx changeset status --output=/dev/null; node -e "const c=require('./.changeset/config.json'); if(c.changelog!==false||c.access!=='public'||c.baseBranch!=='main') process.exit(1)"; node -e "if(!require('./package.json').scripts.release) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `@changesets/cli@2.31.0` presente em devDependencies
    - `.changeset/config.json` com changelog:false, access:public, baseBranch:main
    - `npx changeset status` roda sem erro
    - package.json tem script `release` = `changeset publish`
    - `package.json.version` continua `1.0.34-alpha` (nao editado a mao)
  </acceptance_criteria>
  <done>Changesets esta instalado e configurado para preservar o CHANGELOG manual, com script `release` pronto para o CI.</done>
</task>

<task type="auto">
  <name>Task 2: Criar .github/workflows/release.yml com provenance (OIDC) e gate de Environment</name>
  <read_first>
    .github/workflows/docs.yml (padrao OIDC id-token:write ja usado no repo),
    .github/workflows/ci.yml (steps de build/check reaproveitaveis),
    05-RESEARCH.md Code Examples (release.yml esqueleto) e Architecture Patterns Pattern 3 (gate D-03 via Environment)
  </read_first>
  <files>.github/workflows/release.yml</files>
  <action>
    Criar um workflow NOVO e SEPARADO de ci.yml/docs.yml (D-04), disparando so em `push` para `main` (nunca em `pull_request` de fork — reduz superficie de vazamento do token, research Security Domain). Estrutura conforme o esqueleto do Code Examples do research:
    - `concurrency` por workflow+ref;
    - `permissions`: contents write, pull-requests write, id-token write (o id-token write habilita `--provenance`, reaproveitando o padrao OIDC do docs.yml — D-04);
    - job `release` em `ubuntu-latest` com `environment` npm-publish (gate humano da 1a release — D-03, estrategia (a) do research: Environment com required reviewer permanente; aprovacao de 1 clique nas releases seguintes; documentar essa escolha num comentario do workflow);
    - steps: checkout (fetch-depth 0), setup-node 22.x com cache npm, `npm ci`, `npm run build`, `npm run check:package`, e por fim `uses: changesets/action@v1` com input `publish: npm run release`;
    - env do step da action: GITHUB_TOKEN a partir de secrets.GITHUB_TOKEN, NPM_TOKEN e NODE_AUTH_TOKEN a partir de secrets.NPM_TOKEN, e NPM_CONFIG_PROVENANCE true.
    Pinar a action por tag major `changesets/action@v1` (org oficial `changesets/`, mesma dos mantenedores do CLI — research Package Legitimacy Audit). NUNCA expor NPM_TOKEN em `run:`/echo (nao logar o secret).
  </action>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/release.yml')); j=d['jobs']['release']; assert j.get('environment')=='npm-publish'; assert d['permissions']['id-token']=='write'; assert any('changesets/action@v1' in str(s.get('uses','')) for s in j['steps'])" && grep -q "NPM_CONFIG_PROVENANCE" .github/workflows/release.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/release.yml` existe, separado de ci.yml/docs.yml, disparando so em push para main
    - permissions inclui id-token:write; env do step tem NPM_CONFIG_PROVENANCE:true
    - job usa `environment: npm-publish` (gate D-03) e `changesets/action@v1` (pin major)
    - publish input roda `npm run release`; NPM_TOKEN nunca aparece em run/echo
  </acceptance_criteria>
  <done>O workflow de release existe, separado, com provenance via OIDC e gate de Environment, pronto para publicar quando houver changesets/Version PR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| laptop -> GitHub Actions | Codigo/segredos cruzam para o runner de CI que publica |
| GitHub Actions -> registry npm | O tarball atestado (provenance) cruza para o registry publico |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Information Disclosure | NPM_TOKEN em log/workflow | high | mitigate | Token granular de escopo minimo (so publish de @iamcalegari/mongoat) como secret do repo; release.yml so dispara em push:main (nunca PR de fork); token nunca ecoado em run/echo |
| T-05-04 | Elevation of Privilege | provenance/OIDC | medium | mitigate | Provenance via `npm publish --provenance` + `id-token: write` (Sigstore/OIDC gerenciado pelo npm, nao hand-rolled); reaproveita padrao ja validado em docs.yml |
| T-05-05 | Tampering | publish acidental de branch nao confiavel | high | mitigate | `release.yml` restrito a push:branches:[main] + gate de Environment npm-publish com required reviewer (D-03) |
| T-05-SC | Tampering | npm/action installs (@changesets/cli, changesets/action) | medium | mitigate | Gate de legitimidade ja executado no research (ambos OK, sem [ASSUMED]/[SUS] -> sem checkpoint bloqueante); action pinada @v1 na org oficial `changesets/` |
</threat_model>

<verification>
- `npx changeset status` roda sem erro
- `.changeset/config.json` valida (changelog:false/access:public/baseBranch:main)
- `release.yml` parseia como YAML valido com environment + id-token:write + provenance
- Nenhum publish executado nesta wave (so setup)
</verification>

<success_criteria>
Pipeline changesets pronto e validado localmente; workflow de release separado com provenance e gate de Environment; NPM_TOKEN e o Environment npm-publish documentados como setup humano (user_setup) — bloqueiam o 1o publish ate existirem.
</success_criteria>

<output>
Create `.planning/phases/05-estabiliza-o-de-api-e-release-v1-0/05-02-SUMMARY.md` when done
</output>
