---
phase: 05-estabiliza-o-de-api-e-release-v1-0
plan: 04
type: execute
wave: 3
depends_on:
  - 05-03
files_modified:
  - package.json
  - .changeset/pre.json
  - docs/explanation/versioning.md
  - docs/.vitepress/config.mts
  - README.md
autonomous: true
requirements:
  - REL-04
must_haves:
  truths:
    - "O modo pre-release foi encerrado (changeset pre exit) e o package.json bumpou 1.1.0-rc.0 -> 1.1.0 (sufixo rc removido)"
    - "Existe uma pagina de politica semver publicada no site (docs/explanation/versioning.md)"
    - "A pagina esta no nav e no sidebar /explanation/ do VitePress e e linkada pelo README"
    - "docs:build passa verde com a nova pagina"
  artifacts:
    - docs/explanation/versioning.md
    - docs/.vitepress/config.mts
    - README.md
    - package.json
  key_links:
    - "docs/explanation/versioning.md <-> nav + sidebar /explanation/ do config.mts"
    - "README <-> pagina de politica semver publicada"
    - "changeset pre exit + changeset version <-> package.json 1.1.0 (Pitfall 2: exit nao bumpa sozinho)"
---

<objective>
Preparar a versao estavel e documentar a politica semver (REL-04, D-07, D-01): sair do modo pre-release rc e produzir o `1.1.0` final (sem sufixo -rc), e publicar uma pagina de "Stability & Versioning" no site que define o contrato de estabilidade da API publica. Esta wave nao publica no npm — so prepara o bump e a documentacao; o publish gated e a deprecacao ficam no 05-05.

## Phase Goal

**As a** consumidor da 1.1.0, **I want to** ler uma politica semver clara (o que e breaking/minor/patch, qual e a superficie publica), **so that** eu saiba o que esperar de compatibilidade ao atualizar entre versoes.

Purpose: Congelar o contrato exige documentar as regras do contrato; e o bump rc->estavel deve preceder o publish irreversivel do 05-05.
Output: package.json em 1.1.0 + pagina de politica semver no site linkada pelo README.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-estabiliza-o-de-api-e-release-v1-0/05-CONTEXT.md
@.planning/phases/05-estabiliza-o-de-api-e-release-v1-0/05-RESEARCH.md
@package.json
@src/index.ts
@docs/.vitepress/config.mts
@docs/explanation/thin-odm-philosophy.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sair do pre-mode rc e bumpar para 1.1.0 estavel</name>
  <read_first>
    05-RESEARCH.md Pattern 2 (passo 5: pre exit + version) e Pitfall 2 (pre exit nao bumpa sozinho),
    package.json (version 1.1.0-rc.0), .changeset/pre.json
  </read_first>
  <files>.changeset/pre.json, package.json</files>
  <action>
    Rodar `npx changeset pre exit` (grava a intencao de sair do pre-release em `.changeset/pre.json`) e EM SEGUIDA `npx changeset version` (Pitfall 2 — `pre exit` sozinho NAO remove o sufixo; e o `version` seguinte que recalcula) para produzir `package.json.version` === `1.1.0`. Commitar o resultado. NAO editar `package.json.version` a mao. O changeset consolidado (criado no 05-03) continua sendo a fonte do bump minor.
  </action>
  <verify>
    <automated>node -e "const v=require('./package.json').version; if(v!=='1.1.0'){console.error('version='+v);process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json.version` === `1.1.0` (sufixo -rc.N removido)
    - Bump feito via `changeset pre exit` + `changeset version` (nao manual)
  </acceptance_criteria>
  <done>O repo saiu do pre-mode e o package.json esta em 1.1.0, pronto para o publish gated do 05-05.</done>
</task>

<task type="auto">
  <name>Task 2: Escrever a pagina de politica semver e liga-la ao site e ao README</name>
  <read_first>
    docs/.vitepress/config.mts (nav + sidebar /explanation/, linhas 38-82),
    docs/explanation/thin-odm-philosophy.md (tom/estilo das paginas Explanation),
    src/index.ts (o barrel = superficie publica a citar),
    README.md (secao de links para o site, ~linha 143-149)
  </read_first>
  <files>docs/explanation/versioning.md, docs/.vitepress/config.mts, README.md</files>
  <action>
    Criar `docs/explanation/versioning.md` documentando a politica de estabilidade (D-07): a superficie publica contratual e o barrel `src/index.ts`; simbolos `@internal` ficam FORA do contrato; definir o que conta como MAJOR (breaking na superficie publica), MINOR (adicao retrocompativel) e PATCH (fix retrocompativel); explicar a politica de RC (pre-releases no dist-tag `rc` antes de promover `latest`) e a politica de deprecacao (`npm deprecate` de linhas descontinuadas com ponteiro para a migracao). Registrar que a 1.1.0 e a primeira estavel (contexto D-01: as alphas eram 1.0.x-alpha; 1.0.0 seria numericamente menor).
    Ligar a pagina ao site em `docs/.vitepress/config.mts`: adicionar ao `nav` um item (ex.: "Versioning" -> `/explanation/versioning`) e um item no bloco `sidebar['/explanation/']` (apos "Server-side validation"). Referenciar a pagina no README (na secao de links para o site).
  </action>
  <verify>
    <automated>test -f docs/explanation/versioning.md && grep -q "versioning" docs/.vitepress/config.mts && grep -qi "versioning\|stability\|semver" README.md && npm run docs:build</automated>
  </verify>
  <acceptance_criteria>
    - `docs/explanation/versioning.md` existe e define superficie publica (barrel), @internal fora do contrato, e breaking/minor/patch
    - A pagina aparece no `nav` e no `sidebar['/explanation/']` do config.mts
    - README linka a pagina de politica semver
    - `npm run docs:build` passa verde
  </acceptance_criteria>
  <done>A politica semver esta documentada, publicada no site (nav+sidebar) e referenciada no README.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| codigo-fonte -> versao publicada | O bump 1.1.0 fixa o numero que sera publicado no 05-05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Tampering | bump rc->estavel | low | mitigate | Bump via `changeset pre exit` + `version` (Pitfall 2), nao manual; verificado por assert de version === 1.1.0 antes do publish |
| T-05-02 | Information Disclosure | docs de politica semver | low | accept | Documentacao publica por design; nenhum segredo introduzido |
</threat_model>

<verification>
- `package.json.version` === 1.1.0 (pos pre exit + version)
- `docs/explanation/versioning.md` presente e no nav/sidebar
- `npm run docs:build` verde
</verification>

<success_criteria>
package.json em 1.1.0 estavel (pre-mode encerrado) e politica semver documentada/publicada/linkada — pre-requisitos prontos para o publish gated e a deprecacao (05-05).
</success_criteria>

<output>
Create `.planning/phases/05-estabiliza-o-de-api-e-release-v1-0/05-04-SUMMARY.md` when done
</output>
