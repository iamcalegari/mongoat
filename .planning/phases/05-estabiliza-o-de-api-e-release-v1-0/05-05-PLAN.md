---
phase: 05-estabiliza-o-de-api-e-release-v1-0
plan: 05
type: execute
wave: 4
depends_on:
  - 05-04
files_modified:
  - scripts/deprecate-alphas.mjs
autonomous: false
requirements:
  - REL-04
  - REL-01
must_haves:
  truths:
    - "1.1.0 esta publicada no npm no dist-tag latest, com provenance, publicada via CI (nunca do laptop)"
    - "As 34 versoes 1.0.x-alpha estao deprecadas no registry (amostra confirma a mensagem de descontinuacao)"
    - "A deprecacao usou loop de versao EXATA (nao range) — Pitfall 1/Open Question 2"
    - "A publicacao da 1.1.0 foi aprovada explicitamente pelo autor (gate humano, publish irreversivel)"
  artifacts:
    - scripts/deprecate-alphas.mjs
  key_links:
    - "publish 1.1.0 <-> release.yml gated + provenance (D-03/D-04)"
    - "deprecate-alphas.mjs <-> lista real de versoes via `npm view ... versions` (versao exata, nao range) (D-05)"
---

<objective>
Promover a primeira versao estavel `1.1.0` no npm (dist-tag latest) com provenance, sob aprovacao humana explicita (D-03 — publish irreversivel), e deprecar as 34 versoes alpha por versao EXATA (D-05, REL-04). A partir da 1.1.0 o pipeline changesets publica automaticamente no merge do Version PR (REL-01), com a mesma aprovacao de 1 clique do Environment.

## Phase Goal

**As a** consumidor da alpha, **I want to** que `npm install @iamcalegari/mongoat` instale a 1.1.0 estavel e que as alphas apareçam deprecadas apontando para a migracao, **so that** eu atualize com confianca para a linha estavel.

Purpose: E o ponto de nao-retorno da fase — a materializacao da "v1.0" do roadmap como npm 1.1.0.
Output: 1.1.0 em latest com provenance + 34 alphas deprecadas.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-estabiliza-o-de-api-e-release-v1-0/05-CONTEXT.md
@.planning/phases/05-estabiliza-o-de-api-e-release-v1-0/05-RESEARCH.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar o script de deprecacao das alphas por versao EXATA</name>
  <read_first>
    05-RESEARCH.md Pitfall 1 (CRITICO: range-based npm deprecate nao casa prereleases) e Open Question 2 (usar versao exata em qualquer cenario),
    05-CONTEXT.md D-05 (mensagem de deprecacao exata)
  </read_first>
  <files>scripts/deprecate-alphas.mjs</files>
  <action>
    Criar `scripts/deprecate-alphas.mjs`: script Node que obtem a lista real de versoes publicadas via `npm view @iamcalegari/mongoat versions --json`, filtra as que terminam em `-alpha` (as 34 linhas 1.0.x-alpha), e para CADA versao roda `npm deprecate "@iamcalegari/mongoat@<versao-exata>" "<mensagem>"` iterando por versao EXATA — NUNCA por range (`<1.1.0`/`1.0.x-alpha`/`1.x` nao casam prereleases no node-semver e deprecariam zero versoes silenciosamente — Pitfall 1). Mensagem (D-05): "The 1.0.x-alpha line is discontinued - upgrade to the stable release. Migration guide: https://iamcalegari.github.io/mongoat/migration". O script deve logar cada versao processada e suportar um modo dry-run via a variavel de ambiente `DRY_RUN=1`, que apenas IMPRIME os comandos (uma linha por versao, cada uma comecando com `npm deprecate`) sem executar nada. NAO deprecar 1.1.0 nem 1.1.0-rc.0. O `npm deprecate` exige ser owner do pacote (V4 Access Control) — a execucao real e feita pelo autor autenticado (Task 3).
  </action>
  <verify>
    <automated>node --check scripts/deprecate-alphas.mjs && grep -q "versions --json" scripts/deprecate-alphas.mjs && ! grep -qE "deprecate .*@[\"']?[<>]" scripts/deprecate-alphas.mjs && test "$(DRY_RUN=1 node scripts/deprecate-alphas.mjs 2>/dev/null | grep -c "npm deprecate")" = "34"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/deprecate-alphas.mjs` passa `node --check`
    - O script obtem a lista real via `npm view ... versions --json` e itera por versao EXATA (nenhum uso de range em `npm deprecate`)
    - `DRY_RUN=1 node scripts/deprecate-alphas.mjs` imprime EXATAMENTE 34 comandos `npm deprecate` (um por versao 1.0.x-alpha) — confirma funcionalmente que o script encontra as 34 alphas, nao zero (guarda contra o Pitfall 1)
    - Usa a mensagem exata do D-05; nao toca 1.1.0/1.1.0-rc.0
  </acceptance_criteria>
  <done>O script de deprecacao por versao exata existe, testado por node --check e por dry-run, pronto para execucao autenticada.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Publicar 1.1.0 estavel no dist-tag latest (GATED, irreversivel)</name>
  <action>Acao humana: disparar/mergear o Version PR que aciona o release.yml e APROVAR explicitamente o gate do Environment npm-publish para publicar `1.1.0` no dist-tag `latest` com provenance (D-03 — publish IRREVERSIVEL; publish no CI, nunca do laptop — D-04). Nenhum arquivo e modificado — e o gate de publish da estavel.</action>
  <what-built>
    package.json em 1.1.0 (05-04), pipeline gated (release.yml + Environment npm-publish), RC ja validado por smoke (05-03).
  </what-built>
  <how-to-verify>
    1. Disparar/mergear o Version PR que aciona `release.yml` para publicar `1.1.0` no dist-tag `latest` com provenance (publish no CI, nunca do laptop — D-04).
    2. APROVAR explicitamente o gate do Environment npm-publish (D-03 — publish e IRREVERSIVEL; confirmar a intencao antes de aprovar).
    3. Confirmar: `npm view @iamcalegari/mongoat@1.1.0 version` retorna 1.1.0; `npm view @iamcalegari/mongoat dist-tags` mostra `latest = 1.1.0`.
    4. Confirmar a atestacao de proveniencia na pagina do pacote no npmjs.com (badge/Provenance).
  </how-to-verify>
  <resume-signal>Digite "publicado-1.1.0" quando a estavel estiver em latest com provenance; ou descreva a falha.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Deprecar as 34 alphas por versao exata (irreversivel)</name>
  <action>Acao humana: rodar `scripts/deprecate-alphas.mjs` primeiro em dry-run (confirmar 34 versoes por versao exata, nenhum range), depois executa-lo autenticado como owner do pacote (D-05 — mutacao IRREVERSIVEL no registry). Amostrar `npm view @iamcalegari/mongoat@1.0.34-alpha deprecated` para confirmar. Nenhum arquivo do repo e modificado — e um gate de mutacao do registry.</action>
  <what-built>
    scripts/deprecate-alphas.mjs (loop de versao exata) e a 1.1.0 ja publicada em latest.
  </what-built>
  <how-to-verify>
    1. Rodar primeiro em dry-run: confirmar que o script listou as 34 versoes 1.0.x-alpha e montou um `npm deprecate` por versao exata (nenhum range).
    2. Autenticado como owner do pacote, executar `node scripts/deprecate-alphas.mjs` de verdade (D-05 — mutacao IRREVERSIVEL no registry).
    3. Amostrar a confirmacao: `npm view @iamcalegari/mongoat@1.0.34-alpha deprecated` (e mais 1-2 versoes) deve retornar a mensagem de descontinuacao — se vier `undefined`, o matching falhou (sinal do Pitfall 1: range em vez de versao exata).
  </how-to-verify>
  <resume-signal>Digite "alphas-deprecadas" quando a amostra confirmar a deprecacao; ou descreva a falha.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions -> registry npm | Publish irreversivel da 1.1.0 (dist-tag latest) |
| laptop do autor (owner) -> registry npm | `npm deprecate` muta o estado publicado das 34 alphas |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering | publish irreversivel da 1.1.0 | high | mitigate | Gate humano do Environment npm-publish (checkpoint blocking-human) + publish so via release.yml em push:main com provenance (D-03/D-04) |
| T-05-08 | Tampering | deprecacao silenciosa (range nao casa prereleases) | high | mitigate | Loop de versao EXATA sobre `npm view ... versions` (Pitfall 1/Open Q2); verify proibe range em `npm deprecate`; amostra pos-execucao confirma o `deprecated` |
| T-05-09 | Repudiation | deprecacao irreversivel no registry | medium | accept | Mutacao aceita e desejada (D-05); dry-run antes + amostra depois reduzem o risco de escopo errado |
| T-05-03 | Information Disclosure | NPM_TOKEN no publish | high | mitigate | Token granular de escopo minimo como secret; nunca ecoado; provenance via OIDC |
| T-05-04 | Elevation of Privilege | provenance/OIDC | medium | mitigate | `--provenance` via OIDC/Sigstore gerenciado pelo npm (nao hand-rolled) |
</threat_model>

<verification>
- Pos-publish: `npm view @...@1.1.0 version` === 1.1.0; dist-tags `latest` === 1.1.0; provenance visivel no npmjs.com
- Pos-deprecate: amostra de `npm view @...@1.0.34-alpha deprecated` retorna a mensagem (nao undefined)
- `scripts/deprecate-alphas.mjs`: node --check verde, sem range em npm deprecate
</verification>

<success_criteria>
1.1.0 estavel publicada no npm (latest) com provenance sob aprovacao humana explicita, e as 34 alphas deprecadas por versao exata apontando para o guia de migracao — REL-04 completo; REL-01 (automacao pos-1a-release) habilitado pelo mesmo pipeline.
</success_criteria>

<output>
Create `.planning/phases/05-estabiliza-o-de-api-e-release-v1-0/05-05-SUMMARY.md` when done
</output>
