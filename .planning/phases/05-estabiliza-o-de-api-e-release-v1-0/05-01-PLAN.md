---
phase: 05-estabiliza-o-de-api-e-release-v1-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/database/index.ts
  - src/model/index.ts
  - test/database/proxy-binding.test.ts
  - test/model/registry-config.test.ts
  - CHANGELOG.md
  - MIGRATION.md
  - docs/migration.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - REL-03
must_haves:
  truths:
    - "O barrel público (src/index.ts / Database / Model) não expõe mais defineModel nem Model.create"
    - "A suíte de testes passa verde sem nenhuma referência às APIs removidas"
    - "CHANGELOG.md, MIGRATION.md e docs/migration.md não contêm mais a string de versão-alvo 1.0.0 (substituída por 1.1.0)"
    - "O diff alpha→1.1.0 está registrado: a remoção das 2 APIs deprecated aparece como BREAKING no CHANGELOG e no guia de migração"
    - "A seção CHANGELOG [Unreleased] foi renomeada para [1.1.0] com data"
  artifacts:
    - CHANGELOG.md
    - MIGRATION.md
    - docs/migration.md
    - src/database/index.ts
    - src/model/index.ts
  key_links:
    - "src/database/index.ts + src/model/index.ts (remoção) ↔ testes que exercitavam as APIs (atualizados/removidos)"
    - "MIGRATION.md (fonte editável) ↔ docs/migration.md (cópia publicada) — mantidos em sincronia (convenção D-03 da Fase 4)"
---

<objective>
Auditar e congelar a superfície pública do core antes do RC (REL-03, D-06): resolver os 2 `@deprecated` existentes removendo-os (decisão de planejamento por D-06 + research A4 — breaking aceitável na primeira estável, sem consumidores externos reais), registrar o diff alpha→1.1.0 e reconciliar TODA menção textual "v1.0.0" → "1.1.0" (D-01) nos artefatos de release/documentação.

## Phase Goal

**As a** mantenedor do Mongoat, **I want to** auditar/congelar a API pública e publicar a 1.1.0 estável via pipeline changesets com um RC validado antes, **so that** os consumidores instalem uma versão estável com provenance e semver disciplinado e migrem das alphas deprecadas.

Purpose: Uma API coerente e congelável é pré-requisito do RC; documentar o diff antes de publicar evita congelar inconsistências.
Output: Barrel sem APIs deprecated, testes verdes, CHANGELOG/MIGRATION/docs consistentes em 1.1.0.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-estabiliza-o-de-api-e-release-v1-0/05-CONTEXT.md
@.planning/phases/05-estabiliza-o-de-api-e-release-v1-0/05-RESEARCH.md
@src/index.ts
@src/database/index.ts
@src/model/index.ts
@CHANGELOG.md
@MIGRATION.md
@docs/migration.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover as 2 APIs @deprecated e atualizar os testes que as exercitavam</name>
  <read_first>
    src/database/index.ts (bloco `static defineModel`, ~linha 178-...),
    src/model/index.ts (`static create`, ~linha 423-428),
    test/database/proxy-binding.test.ts (casos que usam `Database.defineModel`, ~linhas 94-121),
    test/model/registry-config.test.ts (casos que usam `Database.defineModel`, ~linhas 178-207)
  </read_first>
  <files>src/database/index.ts, src/model/index.ts, test/database/proxy-binding.test.ts, test/model/registry-config.test.ts</files>
  <action>
    Remover completamente `Database.defineModel` (src/database/index.ts) e `Model.create` (src/model/index.ts) — per D-06 (resolver os @deprecated antes de congelar) e research A4 (remoção preferível: primeira estável, zero consumidores externos). Remover também o JSDoc `@deprecated` associado e quaisquer imports/símbolos que fiquem órfãos apenas por causa da remoção (ex.: helpers usados só por defineModel). NÃO remover o construtor `new Model(...)` nem `registerModel` — são a API canônica que substitui os métodos removidos.
    Nos 2 testes: os casos que exercitavam a API canônica (`new Model(...)`) permanecem intactos; remover/atualizar SOMENTE os casos que chamavam `Database.defineModel`/`Model.create` — a semântica que eles cobriam (config divergente lança MongoatError; mesma config reusa a instância; gating de método permanece) já está coberta pelos casos equivalentes via `new Model(...)` nos mesmos arquivos, então os casos deprecated devem ser removidos, não reescritos para a API nova (evita duplicar asserção).
    Manter arquitetura Proxy/registro intacta (constraint do CLAUDE.md). Não introduzir libs novas.
  </action>
  <verify>
    <automated>! grep -rnE "static +defineModel|static +create\b" src/ && npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep` não encontra mais `static defineModel` em src/database/index.ts nem `static create` em src/model/index.ts
    - `npm run typecheck` passa (sem referências pendentes às APIs removidas)
    - `npm test` verde; nenhum teste referencia `Database.defineModel`/`Model.create`
    - `new Model(...)` continua sendo a via de registro/gating (casos canônicos preservados)
  </acceptance_criteria>
  <done>As 2 APIs deprecated foram removidas, o typecheck e a suíte passam, e os testes não mencionam mais as APIs removidas.</done>
</task>

<task type="auto">
  <name>Task 2: Registrar o diff alpha→1.1.0 e reconciliar as strings de versão (v1.0.0 → 1.1.0)</name>
  <read_first>
    CHANGELOG.md (seção [Unreleased] e rodapé de links, linhas 11-90),
    MIGRATION.md (cabeçalho e status, linhas 1-21),
    docs/migration.md (cabeçalho/status, linhas 1-20),
    .planning/ROADMAP.md (goal + success criteria da Fase 5, linhas 154-163),
    05-RESEARCH.md §Runtime State Inventory (lista exata das fontes a atualizar)
  </read_first>
  <files>CHANGELOG.md, MIGRATION.md, docs/migration.md, .planning/ROADMAP.md</files>
  <action>
    Auditar o barrel `src/index.ts` (~30 exports) contra o CHANGELOG/MIGRATION e confirmar que TODAS as breaking changes documentadas cobrem a superfície pública real (D-06 — auditoria manual guiada, não AST diffing; research Don't Hand-Roll). Adicionar ao CHANGELOG (seção Removed) e ao MIGRATION uma entrada BREAKING nova para a remoção de `Database.defineModel` e `Model.create`, com before/after apontando para `new Model(...)` como substituto.
    Renomear a seção CHANGELOG `## [Unreleased]` para `## [1.1.0] — <data de hoje>`, ajustar o texto introdutório que diz "Work toward the first stable v1.0.0" e o link de comparação do rodapé para refletir `1.1.0` (a tag de comparação parte de `v1.0.34-alpha`).
    Substituir toda menção da versão-alvo estável "v1.0.0"/"1.0.0"/"v1.0.0-rc" por "1.1.0"/"1.1.0-rc.0" (D-01) em CHANGELOG.md, MIGRATION.md, docs/migration.md e no goal/success-criteria da Fase 5 em .planning/ROADMAP.md. NÃO alterar as referências legítimas a `1.0.34-alpha` (histórico) nem strings de versões de terceiros (`keepachangelog 1.1.0`, `semver 2.0.0`, engines `20.19.0`/`22.12.0`).
    Manter MIGRATION.md e docs/migration.md sincronizados (mesma convenção de dupla manutenção da Fase 4 — D-03).
  </action>
  <verify>
    <automated>! grep -rnE "v?1\.0\.0([^0-9]|$)" CHANGELOG.md MIGRATION.md docs/migration.md && sed -n '/### Phase 5:/,/### Phase 6:/p' .planning/ROADMAP.md | grep -q "1\.1\.0" && ! sed -n '/### Phase 5:/,/### Phase 6:/p' .planning/ROADMAP.md | grep -qE "v?1\.0\.0([^0-9]|$)" && grep -c "## \[1.1.0\]" CHANGELOG.md && npm run docs:build</automated>
  </verify>
  <acceptance_criteria>
    - Nenhuma ocorrência de "v1.0.0"/"1.0.0" (versão-alvo) remanescente em CHANGELOG.md, MIGRATION.md, docs/migration.md
    - CHANGELOG tem uma seção `## [1.1.0]` datada (ex-`[Unreleased]`) e uma entrada BREAKING de remoção de defineModel/Model.create
    - MIGRATION.md e docs/migration.md documentam a remoção com before/after (`new Model(...)`)
    - .planning/ROADMAP.md (Fase 5) reflete 1.1.0 no goal/success criteria
    - `npm run docs:build` passa (migration page válida)
  </acceptance_criteria>
  <done>O diff alpha→1.1.0 está registrado, a API está auditada/congelável, e todas as strings de versão-alvo foram reconciliadas para 1.1.0 nos 4 artefatos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| código-fonte → tarball publicado | O que sai do barrel `src/index.ts` vira contrato público congelado na 1.1.0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | superfície pública (barrel) | medium | mitigate | Auditoria manual do barrel guiada por CHANGELOG/MIGRATION (D-06) antes de congelar; remoção deliberada e documentada das APIs deprecated |
| T-05-02 | Information Disclosure | CHANGELOG/MIGRATION | low | accept | Docs de release são públicas por design; nenhum segredo introduzido nesta fase |
</threat_model>

<verification>
- `npm run typecheck && npm test` verde após a remoção
- `npm run docs:build` verde após a reconciliação de versão
- `grep` confirma ausência das APIs removidas em src/ e da versão-alvo antiga nos docs
</verification>

<success_criteria>
API pública auditada e congelável (sem `@deprecated` pendentes), diff alpha→1.1.0 registrado, e CHANGELOG/MIGRATION/docs/ROADMAP consistentes em 1.1.0.
</success_criteria>

<output>
Create `.planning/phases/05-estabiliza-o-de-api-e-release-v1-0/05-01-SUMMARY.md` when done
</output>
