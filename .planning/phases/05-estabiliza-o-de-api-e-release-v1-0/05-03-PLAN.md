---
phase: 05-estabiliza-o-de-api-e-release-v1-0
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - package.json
  - .changeset/pre.json
  - scripts/smoke-rc.mjs
autonomous: false
requirements:
  - REL-03
must_haves:
  truths:
    - "Um dry-run real de `changeset version` numa branch descartavel confirmou o bump 1.0.34-alpha -> 1.1.0 (salvaguarda Open Question 1) e foi revertido"
    - "Existe UM changeset consolidado tipo minor para a 1.1.0"
    - "O modo pre-release rc esta ativo (.changeset/pre.json) e o package.json bumpou para 1.1.0-rc.0"
    - "1.1.0-rc.0 foi publicado no dist-tag rc (NAO latest) com provenance, via o release.yml gated"
    - "O smoke script instala @rc e importa em CJS e ESM com sucesso"
  artifacts:
    - scripts/smoke-rc.mjs
    - .changeset/pre.json
    - package.json
  key_links:
    - "publish do RC <-> release.yml gated + provenance (nunca do laptop, D-04)"
    - "smoke-rc.mjs <-> tarball realmente publicado no dist-tag rc (D-02)"
---

<objective>
Publicar o Release Candidate `1.1.0-rc.0` no dist-tag `rc` e valida-lo end-to-end antes de promover a estavel (REL-03, D-02). Inclui a salvaguarda de dry-run real do `changeset version` (Open Question 1 do research) antes de confiar na mecanica de bump, e um smoke de import CJS+ESM contra o tarball realmente publicado.

## Phase Goal

**As a** consumidor da alpha, **I want to** poder instalar `@iamcalegari/mongoat@rc` e importa-lo em CJS e ESM antes da estavel sair, **so that** a 1.1.0 estavel seja promovida so depois de o tarball ter sido validado de verdade.

Purpose: O RC e a rede de seguranca antes do publish irreversivel da estavel; valida o pipeline (05-02) e a API congelada (05-01) num publish real de baixo risco (dist-tag rc, superseedable por rc.1).
Output: 1.1.0-rc.0 publicado no dist-tag rc + smoke CJS/ESM verde.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-estabiliza-o-de-api-e-release-v1-0/05-CONTEXT.md
@.planning/phases/05-estabiliza-o-de-api-e-release-v1-0/05-RESEARCH.md
@package.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run do changeset version (Open Question 1), changeset consolidado da 1.1.0 e smoke script</name>
  <read_first>
    05-RESEARCH.md Open Questions #1 (dry-run recomendado) e Pattern 1 (changeset consolidado),
    05-RESEARCH.md Code Examples (smoke test pos-RC, dual CJS/ESM),
    package.json (version atual, exports map CJS/ESM)
  </read_first>
  <files>.changeset/ (um arquivo .md novo), scripts/smoke-rc.mjs</files>
  <action>
    Numa branch descartavel, criar um changeset temporario tipo minor e rodar `npx changeset version`, inspecionar o `package.json` resultante e confirmar que bumpou `1.0.34-alpha` -> `1.1.0` (salvaguarda da Open Question 1 — a matematica do semver bate, mas confirmar a integracao @changesets/cli <-> package.json real vale 1 comando); depois DESCARTAR a branch (git checkout/reset) sem manter o bump.
    De volta ao branch de trabalho, criar UM changeset consolidado via `npx changeset add` tipo `minor` com descricao curta tipo "First stable release (1.1.0) - see CHANGELOG.md" (Pattern 1: com changelog:false o corpo nao vira changelog publico; a unica funcao e o `changeset version`/`status` calcularem o bump minor correto).
    Criar `scripts/smoke-rc.mjs`: script Node que, num diretorio temporario, roda `npm init -y`, `npm install @iamcalegari/mongoat@rc`, e verifica o import nos dois formatos — `require('@iamcalegari/mongoat')` (CJS) e `import ... from '@iamcalegari/mongoat'` (ESM) — checando que `Database` e `Model` sao funcoes; sair com codigo != 0 em qualquer falha. Este script fecha o D-02 (smoke CJS+ESM contra o tarball publicado) e sera rodado na Task 3.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const f=fs.readdirSync('.changeset').filter(x=>x.endsWith('.md')&&x!=='README.md');if(!f.length)process.exit(1)" && node --check scripts/smoke-rc.mjs && node -e "const s=require('semver');process.exit(s.inc('1.0.34-alpha','minor')==='1.1.0'&&s.inc('1.0.34-alpha','preminor','rc')==='1.1.0-rc.0'?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Dry-run confirmou 1.0.34-alpha -> 1.1.0 e foi revertido (nenhum bump acidental no branch de trabalho)
    - Existe exatamente um changeset .md consolidado tipo minor em .changeset/
    - scripts/smoke-rc.mjs existe e passa `node --check` (sintaxe valida), cobrindo import CJS e ESM
  </acceptance_criteria>
  <done>A mecanica de bump esta confirmada por dry-run, o changeset consolidado da 1.1.0 existe, e o smoke script esta pronto.</done>
</task>

<task type="auto">
  <name>Task 2: Entrar em pre-mode rc e gerar o bump 1.1.0-rc.0</name>
  <read_first>
    05-RESEARCH.md Pattern 2 (fluxo RC->estavel, comandos exatos) e Pitfall 2 (pre exit nao bumpa sozinho)
  </read_first>
  <files>.changeset/pre.json, package.json</files>
  <action>
    Rodar `npx changeset pre enter rc` (cria `.changeset/pre.json`, gravando a intencao de pre-release com tag rc). Em seguida rodar `npx changeset version` para aplicar o bump `1.0.34-alpha` -> `1.1.0-rc.0` no `package.json` (research Pattern 2, verificado: `semver.inc('1.0.34-alpha','preminor','rc') === '1.1.0-rc.0'`). Commitar o resultado (package.json + .changeset/pre.json + consumo do changeset). NAO editar `package.json.version` a mao. Preparar o push/Version PR que dispara o `release.yml` — a publicacao em si e a Task 3 (gated).
  </action>
  <verify>
    <automated>node -e "const v=require('./package.json').version; if(v!=='1.1.0-rc.0'){console.error('version='+v);process.exit(1)}" && test -f .changeset/pre.json</automated>
  </verify>
  <acceptance_criteria>
    - `.changeset/pre.json` existe (modo pre-release rc ativo)
    - `package.json.version` === `1.1.0-rc.0` (bump via changeset version, nao manual)
    - Mudanca commitada e pronta para disparar o release.yml
  </acceptance_criteria>
  <done>O repo esta em pre-mode rc com o package.json bumpado para 1.1.0-rc.0, pronto para o publish gated.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Publicar 1.1.0-rc.0 no dist-tag rc (gated) e validar o smoke CJS+ESM</name>
  <action>Acao humana: aprovar o gate do Environment npm-publish para o release.yml publicar `1.1.0-rc.0` no dist-tag `rc` com provenance (publish no CI, nunca do laptop — D-04); depois rodar `node scripts/smoke-rc.mjs` para validar import CJS+ESM contra o tarball publicado (D-02). Nenhum arquivo e modificado — e um gate de publish (write irreversivel no registry, dist-tag rc superseedable).</action>
  <what-built>
    Pipeline gated pronto (release.yml + Environment npm-publish), package.json em 1.1.0-rc.0, changeset consolidado aplicado e smoke script (scripts/smoke-rc.mjs) preparado.
  </what-built>
  <how-to-verify>
    1. Confirmar que o secret NPM_TOKEN e o Environment `npm-publish` (required reviewer) existem (user_setup do Plano 05-02) — sem eles o publish falha.
    2. Disparar/mergear o fluxo que aciona `release.yml` para publicar o RC no dist-tag `rc` com provenance (o publish roda no CI, nunca do laptop — D-04). Aprovar o gate do Environment npm-publish quando solicitado (acao humana, publish e um write no registry publico).
    3. Confirmar a publicacao: `npm view @iamcalegari/mongoat@1.1.0-rc.0 dist-tags` deve mostrar `rc` (e NAO mover `latest`).
    4. Rodar o smoke real: `node scripts/smoke-rc.mjs` — deve instalar `@iamcalegari/mongoat@rc` e importar em CJS e ESM com sucesso (D-02). Rodar tambem `npm run check:package` localmente (attw + publint verdes).
    5. Opcional: compilar o quick start do README contra o pacote empacotado.
  </how-to-verify>
  <resume-signal>Digite "rc-ok" quando o RC estiver publicado no dist-tag rc e o smoke CJS+ESM passar; ou descreva a falha.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions -> registry npm | Publish do RC cruza para o registry publico (dist-tag rc) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Tampering | publish do RC (irreversivel por versao) | medium | mitigate | RC vai para dist-tag `rc` (nao `latest`); superseedable por rc.1; publish so via release.yml gated no CI com provenance (D-02/D-04); smoke valida o tarball antes de promover |
| T-05-05 | Tampering | publish acidental / dist-tag errado | high | mitigate | Gate humano do Environment npm-publish (checkpoint) antes do write no registry; verificacao explicita de que `latest` NAO se moveu |
| T-05-04 | Elevation of Privilege | provenance/OIDC | medium | mitigate | `--provenance` via NPM_CONFIG_PROVENANCE + id-token:write (Sigstore/OIDC, nao hand-rolled) |
</threat_model>

<verification>
- Dry-run confirmou o bump antes de confiar na mecanica
- `package.json.version` === 1.1.0-rc.0 (pre-mode)
- Pos-publish: `npm view @...@1.1.0-rc.0 dist-tags` mostra rc; `latest` inalterado
- `node scripts/smoke-rc.mjs` verde (import CJS + ESM) + `npm run check:package` verde
</verification>

<success_criteria>
1.1.0-rc.0 publicado no dist-tag rc com provenance, validado por smoke CJS+ESM real contra o tarball publicado — pre-requisito verificado para a promocao estavel (05-05).
</success_criteria>

<output>
Create `.planning/phases/05-estabiliza-o-de-api-e-release-v1-0/05-03-SUMMARY.md` when done
</output>
