---
phase: 02-sistema-de-hooks-completo-e-api-thin-nativa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/hooks.ts
  - src/model/hooks.ts
  - src/model/index.ts
  - src/types/model.ts
  - src/index.ts
  - test/model/hooks-pre-order.test.ts
  - test/model/hooks-post-order.test.ts
  - test/model/hooks-recursion-guard.test.ts
autonomous: true
requirements: [HOOK-01, HOOK-02, HOOK-05]

must_haves:
  truths:
    - "Dev registra múltiplos pre hooks no mesmo método (via construtor E via .pre() encadeável) e TODOS executam, em ordem de registro, aguardados sequencialmente — inclusive em insertMany (D-01, D-02, HOOK-01)."
    - "Dev registra múltiplos post hooks; cada um recebe ctx.result; um post-hook que retorna valor transforma o resultado entregue ao caller, retornar undefined apenas observa (D-04, HOOK-02)."
    - "Hooks recebem um objeto ctx explícito (fim do this=documento); ctx carrega method/model + filter/document(s)/options/result conforme o método (D-03)."
    - "Um hook que chama um método do próprio model roda em modo raw (sem re-disparar hooks) — não estoura a pilha nem entra em loop (D-07, HOOK-05)."
    - "insertMany paraleliza ENTRE documentos (Promise.all preservado da Fase 1) mas roda múltiplos hooks do MESMO documento sequencialmente (for...of)."
  artifacts:
    - "src/types/hooks.ts — HookFn, PostHookEntry, HookConfig, HookRegistry, BaseHookContext, HookContextMap"
    - "src/model/hooks.ts — runPreHooks, runPostHooks (transform-via-retorno), buildContext"
    - "src/model/index.ts — registry hooks (substitui preMethod), símbolo kHookContext (AsyncLocalStorage por instância), pre()/post() acumulativos e encadeáveis, executeHooked + variantes raw por método"
    - "test/model/hooks-pre-order.test.ts, hooks-post-order.test.ts, hooks-recursion-guard.test.ts"
  key_links:
    - "ctx.options/ctx.document/ctx.filter é a MESMA referência passada ao driver — mutação de pre-hook chega ao driver (base para API-01 no Plan 03)."
    - "Todo método CRUD entra checando store.raw e roda o corpo hookado dentro de kHookContext.run({ raw: true }, ...) — é o mecanismo do guard de recursão."
    - "hooks registry é inicializado com um entry { pre: [], post: [] } para cada um dos 12 valores de METHODS; construtor popula ANTES do return de registerModel."
---

<objective>
Substituir o `preMethod: Record<METHODS, Function>` (1 handler, `.bind(doc)(options)`, sem post-hooks) por um pipeline pre/post completo com múltiplos handlers, contrato de contexto `ctx` explícito, registro dual (declarativo no construtor + encadeável), execução sequencial em ordem de registro e um guard de recursão via `AsyncLocalStorage` por instância de Model.

Purpose: É a fundação de toda a fase — HOOK-03/HOOK-04 (Plan 02) e o passthrough de options/ctx.options (Plan 03) assentam sobre este pipeline. Materializa D-01, D-02, D-03, D-04 e D-07.
Output: `src/types/hooks.ts` e `src/model/hooks.ts` novos; `src/model/index.ts` com os 12 métodos CRUD reescritos para o pipeline; três suítes de integração verdes contra Mongo real.
</objective>

## Phase Goal (MVP — user story)

**As a** desenvolvedor que consome a Mongoat, **I want to** registrar hooks pre/post completos em cada método CRUD e acessar o driver nativo diretamente com options e retornos precisamente tipados, **so that** eu tenha a produtividade de um ODM sem abrir mão do controle total do MongoDB.

> Derivada fielmente da linha `**Goal**` da Fase 2 no ROADMAP.md (declaração de capacidade centrada em "o dev") e do core value do projeto. Este plano entrega a primeira fatia end-to-end: hooks pre/post funcionando por método público contra Mongo real.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sistema-de-hooks-completo-e-api-thin-nativa/02-CONTEXT.md
@.planning/phases/02-sistema-de-hooks-completo-e-api-thin-nativa/02-RESEARCH.md
@.planning/phases/02-sistema-de-hooks-completo-e-api-thin-nativa/02-PATTERNS.md

@src/model/index.ts
@src/types/model.ts
@src/utils/enums.ts
@src/errors/index.ts
@src/index.ts
@test/model/insertmany-hooks.test.ts
</context>

## Artifacts this plan produces

Símbolos NOVOS (o source-grounding não deve flaggar como drift):

- **`src/types/hooks.ts`** (novo): `HookFn<Ctx>`, `PostHookEntry<Ctx>` (`{ fn; fireAndForget? }`), `HookConfig<Ctx>` (`{ pre?: HookFn[]; post?: (HookFn | PostHookEntry)[] }` — forma declarativa por método no construtor), `HookRegistry<ModelType>`, `BaseHookContext<ModelType>` (`{ method, model }`), `HookContextMap<ModelType>` (mapa indexado por `METHODS` — shape do ctx por método, ver RESEARCH Pattern 3).
- **`src/model/hooks.ts`** (novo): `runPreHooks(hooks, ctx)`, `runPostHooks(hooks, ctx)` (sequencial, transform-via-retorno; SEM branch fireAndForget ainda — isso é do Plan 02), `buildContext(method, model, fields)`.
- **`src/model/index.ts`**: campo privado `hooks: HookRegistry<ModelType>` (substitui `preMethod`, que é REMOVIDO — breaking, aceitável em alpha); símbolo `kHookContext` (`AsyncLocalStorage<{ raw: true }>` por instância); `pre(method, fn)` reescrito (acumula + `ctx` + retorna `this`); `post(method, fn, options?)` novo (encadeável); helper privado `executeHooked(method, ctx, rawFn)`; variantes raw por método (naming à discrição: `rawInsert`, `rawUpdate`, ...).
- **`src/types/model.ts`**: `CreateModelProps.hooks?: { [M in METHODS]?: HookConfig<...> }`.
- **`src/index.ts`**: re-export dos tipos públicos de hooks.

<tasks>

<task type="auto">
  <name>Task 1: Testes RED — ordem/sequencialidade de pre e post hooks + ctx.result + guard de recursão</name>
  <files>test/model/hooks-pre-order.test.ts, test/model/hooks-post-order.test.ts, test/model/hooks-recursion-guard.test.ts</files>
  <read_first>
    - test/model/insertmany-hooks.test.ts (analog EXATO — setup Database real, collectionName único, resetRegistry no afterAll, setupCollection antes de exercitar; ver PATTERNS.md "Testes de integração")
    - test/model/crud-happy-path.test.ts (analog — encadeamento de métodos públicos por model único)
    - .planning/phases/02-sistema-de-hooks-completo-e-api-thin-nativa/02-RESEARCH.md (Pattern 1/2/3 — assinatura ctx, ordem, transform; Validation Architecture)
  </read_first>
  <action>
    Escrever as três suítes de integração (Vitest + testcontainers, sem mocks do driver) que definem o contrato deste plano, seguindo EXATAMENTE o template de setup/teardown do analog: `collectionName` único por teste, `Database.resetRegistry()` no afterAll, `db.setupCollection(model as unknown as Model)` antes de exercitar. Cabeçalho de cada arquivo referenciando o requisito coberto (HOOK-01/HOOK-02/HOOK-05).

    hooks-pre-order.test.ts (HOOK-01): registrar dois pre hooks no mesmo método `insert` — um via campo `hooks` do construtor, outro via `.pre(METHODS.INSERT, ...)` encadeável — cada um fazendo push de uma etiqueta a um array externo de ordem; assertar que a ordem final é [construtor, encadeável] e que ambos rodaram. Segundo caso: dois pre hooks assíncronos onde o segundo LÊ um campo de `ctx.document` escrito pelo primeiro — assertar persistência coerente (prova sequencialidade, não paralelismo). Terceiro caso: mesmo padrão em `insertMany` com múltiplos documentos — assertar que todos os documentos refletem ambos os hooks E que a mutação de cada doc é independente (paralelismo entre docs, sequencial dentro do doc).

    hooks-post-order.test.ts (HOOK-02): registrar dois post hooks em `insert`; assertar que ambos rodam em ordem e recebem `ctx.result` (o documento inserido). Caso de transformação (D-04): um post hook que RETORNA um objeto derivado — assertar que o valor devolvido por `await model.insert(...)` é o retornado pelo hook; e um post hook que não retorna nada — assertar que o resultado permanece o cru do driver (observa, não transforma).

    hooks-recursion-guard.test.ts (HOOK-05): model com um pre hook em `insert` cujo corpo chama `model.find(...)` (ou `model.total()`), método que também tem um hook registrado; assertar que `await model.insert(...)` resolve normalmente (sem RangeError/stack overflow) e que os hooks do método aninhado NÃO re-dispararam (contador de execuções do hook aninhado permanece no esperado — a chamada aninhada roda em modo raw). Usar o padrão de asserção de erro de PATTERNS.md ("Padrão de asserção de erro tipado") onde aplicável.
  </action>
  <verify>
    <automated>npx vitest run test/model/hooks-pre-order.test.ts test/model/hooks-post-order.test.ts test/model/hooks-recursion-guard.test.ts --reporter=dot; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - Os três arquivos existem, são coletados pelo Vitest e FALHAM (RED esperado — a implementação ainda não existe). O gate `test $? -ne 0` confirma o RED.
    - Cada suíte usa Database real (testcontainers), collectionName único e resetRegistry no afterAll — sem mocks do driver.
    - Nenhum uso de watch mode; comando é `vitest run`.
  </acceptance_criteria>
  <done>Três suítes RED escritas contra Mongo real, cobrindo ordem/sequencialidade pre, ordem+ctx.result+transform post, e guard de recursão.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tipos de hook + runner do pipeline (src/types/hooks.ts, src/model/hooks.ts)</name>
  <files>src/types/hooks.ts, src/model/hooks.ts, src/types/model.ts, src/index.ts</files>
  <read_first>
    - src/types/model.ts (convenção de import/naming de tipos, sufixo Props, HookContextMap segue o padrão de mapped type de ModelValidationSchema — PATTERNS.md src/types/hooks.ts)
    - src/model/index.ts (imports de options do driver já presentes; padrão de símbolo k*)
    - .planning/phases/02-sistema-de-hooks-completo-e-api-thin-nativa/02-RESEARCH.md (Pattern 1/2/3 — assinaturas exatas alvo)
    - src/index.ts (barrel de re-export)
  </read_first>
  <behavior>
    - runPreHooks executa hooks em `for...of` + `await`, ordem do array, e propaga o primeiro throw (aborta a cadeia).
    - runPostHooks executa em `for...of` + `await`; se o hook retorna valor !== undefined, atribui a ctx.result; se retorna undefined, mantém ctx.result. (SEM branch fireAndForget neste plano.)
    - HookContextMap tipa corretamente cada método: pre<M>(m, fn) infere o ctx certo a partir de M (checado por tsc no Plan 03; aqui garantir que compila).
  </behavior>
  <action>
    Criar src/types/hooks.ts seguindo o agrupamento de imports do projeto (bloco `mongodb` primeiro — `FindOptions`, `InsertOneOptions`, `BulkWriteOptions`, `UpdateOptions`, `DeleteOptions`, `FindOneAndUpdateOptions`, `FindOneAndDeleteOptions`, `CountDocumentsOptions`, `AggregateOptions`, `AnyBulkWriteOperation`, `Filter`, `UpdateFilter`, `WithId`, `OptionalUnlessRequiredId`, `ObjectId`, `Document`, e os `*Result` do driver — depois `@/utils/enums`). Definir: `HookFn<Ctx>` como `(ctx: Ctx) => void | unknown | Promise<void | unknown>`; `PostHookEntry<Ctx>` como `{ fn: HookFn<Ctx>; fireAndForget?: boolean }` (o campo fireAndForget existe no tipo desde já, mas só será CONSUMIDO no Plan 02); `HookConfig<Ctx>` para a forma declarativa do construtor (`pre?`, `post?` aceitando `HookFn` ou `PostHookEntry`); `BaseHookContext<ModelType>` (`method: METHODS; model: Model<ModelType>`); `HookContextMap<ModelType>` como o mapped/lookup type indexado por `METHODS` exatamente como RESEARCH Pattern 3 (cada método com seus campos filter/document(s)/update/pipeline/operations/options/result apropriados); `HookRegistry<ModelType>` como `{ [M in METHODS]: { pre: HookFn<HookContextMap<ModelType>[M]>[]; post: PostHookEntry<HookContextMap<ModelType>[M]>[] } }`. Importar `Model` como type-only de `@/model` para o BaseHookContext (evitar ciclo em runtime — usar `import type`).

    Criar src/model/hooks.ts: `runPreHooks(hooks, ctx)` e `runPostHooks(hooks, ctx)` conforme o behavior acima; `buildContext(method, model, fields)` que monta o objeto ctx base + campos. runPostHooks NÃO deve conter branch de fireAndForget nem parâmetro onHookError neste plano (fica isolado para o Plan 02 adicionar num único ponto). Comentar que a convenção de transformação é return-value (D-04, RESEARCH A1 — decisão travada aqui).

    Em src/types/model.ts: adicionar `hooks?` a `CreateModelProps<ModelType>` tipado como `{ [M in METHODS]?: HookConfig<HookContextMap<ModelType>[M]> }` (import dos novos tipos de `@/types/hooks`). Não alterar `ModelSetup` (caminho deprecated).

    Em src/index.ts: re-exportar os tipos públicos (`HookFn`, `PostHookEntry`, `HookConfig`, `HookContextMap`) via `export type`, seguindo a organização alfabética já usada no barrel.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passa: os novos tipos compilam e `CreateModelProps.hooks` aceita a forma declarativa por método.
    - runPostHooks implementa transform-via-retorno (undefined = observa) e NÃO tem branch fireAndForget nem parâmetro onHookError.
    - src/types/hooks.ts usa `import type { Model }` (sem import de runtime que crie ciclo).
    - Tipos públicos de hooks re-exportados em src/index.ts.
  </acceptance_criteria>
  <done>Contrato de tipos e runner do pipeline prontos e type-checkando; CreateModelProps aceita `hooks` declarativo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reescrever os 12 métodos CRUD para o pipeline pre/post + registro dual + guard de recursão</name>
  <files>src/model/index.ts</files>
  <read_first>
    - src/model/index.ts (estado atual completo — preMethod linhas ~176-189, pre() ~374-387, insert ~460-483, insertMany ~485-515; padrões a PRESERVAR: cloneDocumentDefaults, wrapDriverError, getCollectionOrThrow FORA do try)
    - src/model/hooks.ts (runner criado na Task 2)
    - src/types/hooks.ts (contrato ctx da Task 2)
    - .planning/phases/02-sistema-de-hooks-completo-e-api-thin-nativa/02-RESEARCH.md (Pattern 5 — dispatch raw/hooked com AsyncLocalStorage; Pitfall 1/4; Anti-Patterns)
    - test/model/insertmany-hooks.test.ts (o Promise.all ENTRE documentos deve continuar verde)
  </read_first>
  <behavior>
    - Registrar dois pre no mesmo método executa AMBOS em ordem (não mais sobrescreve).
    - Dois post recebem ctx.result; retorno de post transforma, undefined observa.
    - Hook que chama método do próprio model não re-dispara hooks (modo raw) e não estoura a pilha.
    - insertMany: paralelo entre docs, sequencial entre hooks do mesmo doc.
  </behavior>
  <action>
    Remover o campo `preMethod` e substituir por `hooks: HookRegistry<ModelType>` inicializado com `Object.fromEntries(Object.values(METHODS).map((m) => [m, { pre: [], post: [] }]))` (forma compacta recomendada em PATTERNS.md). Adicionar o símbolo privado `kHookContext` (padrão k*) e um campo de instância `[kHookContext] = new AsyncLocalStorage<{ raw: true }>()` (import `{ AsyncLocalStorage } from 'node:async_hooks'` — nativo, sem nova dep). No construtor, DEPOIS de resolver a config e ANTES/junto do `registerModel` return, popular `this.hooks` a partir de `props.hooks` (D-02: entradas do construtor primeiro), normalizando cada post declarativo para `PostHookEntry` (função crua vira `{ fn }`). Manter o construtor SÍNCRONO (nenhum await novo — D-07 da Fase 1).

    Reescrever `pre(method, fn)`: acumular via `this.hooks[method].pre.push(fn)` (não sobrescrever — D-01) e retornar `this` (encadeável — D-02). Adicionar `post(method, fn, options?)`: `this.hooks[method].post.push({ fn, fireAndForget: options?.fireAndForget })` e retornar `this`. Assinaturas usam `ctx` explícito via `HookContextMap` (fim do `this: T` + `args`).

    Introduzir o helper privado `executeHooked(method, ctx, rawFn)`: `await runPreHooks(this.hooks[method].pre, ctx)` → `ctx.result = await rawFn()` → `await runPostHooks(this.hooks[method].post, ctx)` → `return ctx.result`. (NÃO passar onHookError aqui — o Plan 02 acrescenta esse parâmetro num único ponto.)

    Para CADA um dos 12 métodos CRUD, aplicar o dispatch de reentrância (RESEARCH Pattern 5): na entrada, ler `const store = this[kHookContext].getStore()`; se `store?.raw`, chamar diretamente a variante crua (`rawInsert`, etc.) pulando hooks; senão, montar o `ctx` via `buildContext` e retornar `this[kHookContext].run({ raw: true }, () => this.executeHooked(METHODS.X, ctx, () => this.rawX(ctx.<campos>)))`. Extrair a lógica de driver ATUAL de cada método (incluindo `getCollectionOrThrow()` FORA do try, `try/catch` + `wrapDriverError` onde já existe, e o `cloneDocumentDefaults` de insert/insertMany/bulkWrite) para as variantes `rawX`, sem regredir nenhum fix da Fase 1. CRÍTICO (Pitfall 4): as variantes raw devem ler de `ctx.options`/`ctx.filter`/`ctx.document(s)`/`ctx.update`/`ctx.pipeline`/`ctx.operations` — a mesma referência que os pre-hooks podem ter mutado — não dos parâmetros originais.

    insertMany (Pitfall 1 / OQ2 resolvida): preservar `Promise.all(documents.map(...))` para paralelizar ENTRE documentos; DENTRO de cada documento os múltiplos pre hooks rodam via `for...of` + `await` (runPreHooks). Não usar Promise.all para hooks do mesmo documento. O `cloneDocumentDefaults` por documento (WR-06) permanece.

    Manter `getCollectionOrThrow()`, `wrapDriverError`, `cloneDocumentDefaults`, `stableStringify`, `isSameConfig` intactos. NÃO tocar `KModelProxyHandler` nem o enum METHODS.
  </action>
  <verify>
    <automated>npx vitest run test/model/hooks-pre-order.test.ts test/model/hooks-post-order.test.ts test/model/hooks-recursion-guard.test.ts test/model/insertmany-hooks.test.ts test/model/crud-happy-path.test.ts test/database/proxy-binding.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - As três suítes da Task 1 passam (GREEN): pre/post múltiplos em ordem sequencial, ctx.result + transform, guard de recursão sem stack overflow.
    - insertmany-hooks.test.ts e crud-happy-path.test.ts continuam verdes (nenhuma regressão dos fixes da Fase 1; Promise.all entre docs preservado).
    - proxy-binding.test.ts continua verde (gating de allowedMethods intacto para métodos hookados).
    - `preMethod` não existe mais; toda chamada de hook passa por runPreHooks/runPostHooks.
  </acceptance_criteria>
  <done>Os 12 métodos CRUD executam o pipeline pre→driver→post com ctx explícito, registro dual acumulativo e guard de recursão; toda a suíte da fase (até aqui) e as regressões da Fase 1 verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-code → pipeline | Funções de hook fornecidas pelo dev executam DENTRO do pipeline do Model, com acesso mutável a `ctx` (document/filter/options). |
| hook → driver | `ctx.options`/`ctx.filter`/`ctx.document` mutados por um pre-hook cruzam para a chamada nativa ao driver. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Denial of Service | Model — hook chamando método do próprio model | high | mitigate | Guard de recursão via `AsyncLocalStorage` por instância (Pattern 5); chamada aninhada roda em modo raw. Coberto por hooks-recursion-guard.test.ts. |
| T-02-03 | Tampering | Model — pre-hook muta `ctx.options`/`ctx.filter` | low | accept | Decorrência aceita de D-09 (pre-hook PODE ajustar options antes do driver). `ctx` é a única fonte de verdade após os pre-hooks; documentar como comportamento esperado. Sanitização de filtro é SEC-01 (Fase 3). |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | Nenhuma dependência de runtime nova nesta fase — `AsyncLocalStorage` é nativo (`node:async_hooks`) e os tipos de options já vêm de `mongodb@7.0.0` (RESEARCH: Package Legitimacy Audit não se aplica). Sem checkpoint de supply-chain. |
</threat_model>

<verification>
- `npx vitest run test/model/hooks-pre-order.test.ts test/model/hooks-post-order.test.ts test/model/hooks-recursion-guard.test.ts` — verde.
- `npx tsc --noEmit` — verde (novos tipos e registry).
- `npm test` — suíte completa verde (nenhuma regressão da Fase 1).
</verification>

<success_criteria>
- Múltiplos pre e post por método, ordem de registro, aguardados sequencialmente (HOOK-01, HOOK-02) — inclusive insertMany.
- Post-hooks recebem ctx.result; transform-via-retorno opt-in (D-04).
- ctx explícito substitui o this=documento (D-03); registro dual acumulativo (D-01, D-02).
- Guard de recursão impede loop/stack overflow (HOOK-05, D-07).
</success_criteria>

<output>
Create `.planning/phases/02-sistema-de-hooks-completo-e-api-thin-nativa/02-01-SUMMARY.md` when done
</output>
