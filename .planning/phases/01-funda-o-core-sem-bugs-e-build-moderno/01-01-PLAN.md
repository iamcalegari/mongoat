---
phase: 01-funda-o-core-sem-bugs-e-build-moderno
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/errors/index.ts
  - src/index.ts
autonomous: false
requirements: [REL-02, QUAL-04, QUAL-01]
user_setup:
  - service: docker
    why: "Backend de teste (D-13): @testcontainers/mongodb sobe Mongo real em container nas fases de teste (plans 03-05). Docker daemon precisa estar rodando."
    dashboard_config:
      - task: "Garantir que o Docker daemon está ativo (docker ps responde)"
        location: "Ambiente local / CI runner"

must_haves:
  truths:
    - "package.json declara engines.node = \"^20.19.0 || >=22.12.0\" (D-01)"
    - "package.json files contém apenas [\"lib\"] (D-05) — sem src nem tsconfig.json"
    - "json-schema não aparece em dependencies (runtime) — QUAL-04"
    - "ts-jest, ts-node-dev, tsc-alias, tsconfig-paths e typescript-cached-transpile removidos de devDependencies (D-12)"
    - "MongoatError é uma classe que estende Error, exportada no barrel raiz (D-11)"
    - "tsconfig.json target = ES2023 (D-02)"
    - "scripts build (tsdown), check:package (attw+publint), test, test:watch, typecheck, example (tsx) existem em package.json (D-14, D-16, D-12)"
  artifacts:
    - "src/errors/index.ts — classe MongoatError"
    - "package.json — manifesto migrado (engines, files, exports dual, scripts, deps)"
    - "tsconfig.json — target ES2023"
  key_links:
    - "src/index.ts re-exporta MongoatError → consumidores e camadas internas importam de '@/errors'"
    - "package.json exports map aponta para lib/index.cjs|mjs + lib/index.d.cts|mts (produzidos pelo build do plan 02)"
  prohibitions:
    - statement: "NÃO usar \"type\": \"module\" no package.json (dual-package hazard); a saída ESM usa extensão .mjs explícita"
      status: resolved
      verification: "npm pkg get type retorna \"commonjs\" ou o campo é ausente"
    - statement: "NÃO habilitar useUnknownInCatchVariables no tsconfig.json (Pitfall 4 — quebraria os catch blocks fora de escopo, Fase 3/SEC-04)"
      status: resolved
      verification: "grep confirma useUnknownInCatchVariables permanece false em tsconfig.json"
    - statement: "NÃO instalar/atualizar TypeScript para 6.x — manter 5.9.x conforme stack declarado"
      status: resolved
      verification: "npm pkg get devDependencies.typescript inicia com 5.9"
---

<objective>
Estabelecer a fundação de tooling da Fase 1: migrar o manifesto do pacote (`package.json`) para engines Node >=20.19 (D-01), distribuição dual CJS/ESM (D-03), publicação só de `lib/` (D-05) e scripts modernos (build tsdown, check:package, vitest); remover a dependência de runtime `json-schema` (QUAL-04) e as devDependencies mortas (D-12); e criar a classe base de erros `MongoatError` (D-11) que todas as correções desta fase (D-06, D-08, D-10) vão lançar.

Purpose: Este é o slice de fundação — instala o tooling verificado (atrás de checkpoint de legitimidade), fixa o manifesto e cria o único símbolo compartilhado (`MongoatError`) do qual os plans de build (02), teste (03) e correções (04, 05) dependem. Sem ele, nada mais compila ou é testável.
Output: `package.json` migrado, `tsconfig.json` com target ES2023, `src/errors/index.ts` novo e `MongoatError` exportado no barrel raiz.
</objective>

<user_story>
**As a** desenvolvedor que consome o Mongoat (e o mantenedor que publica a lib), **I want to** instalar e importar a lib em projetos CommonJS e ESM com o exports map correto e sem nenhum bug de correção conhecido, **so that** eu tenho uma fundação confiável sobre o driver oficial do MongoDB para construir as próximas fases.

> Este plan entrega a primeira fatia end-to-end da fundação: o manifesto dual + o tooling instalado + o símbolo de erro compartilhado. Após este plan, o build (02) e os testes (03) podem ser construídos, e as correções (04, 05) têm `MongoatError` disponível.
</user_story>

<artifacts_produced>
## Artifacts this phase produces (introduzidos por este plan)

- `MongoatError` — classe exportada (`src/errors/index.ts`), estende `Error`, aceita `{ cause }`.
- Novo diretório de módulo `src/errors/`.
- Scripts npm novos: `check:package`, `test`, `test:watch`, `typecheck` (e `build`/`example` migrados).
- Novo formato de `exports` map (root-only, dual `import`/`require` com `types` primeiro).
- Decisão de escopo: **remoção dos subpath exports** (`./database`, `./model`, `./utils`, `./types`) — ver Task 2.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-funda-o-core-sem-bugs-e-build-moderno/01-CONTEXT.md
@.planning/phases/01-funda-o-core-sem-bugs-e-build-moderno/01-RESEARCH.md
@.planning/phases/01-funda-o-core-sem-bugs-e-build-moderno/01-PATTERNS.md
@package.json
@tsconfig.json
@src/index.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verificar legitimidade dos pacotes de tooling antes de instalar</name>
  <action>Pausar antes de qualquer `npm install`. Apresentar ao humano a lista dos 7 pacotes [SUS] para verificação de legitimidade no npmjs.com (nome exato, repo oficial, versão latest). Nenhum arquivo é modificado nesta task — é um gate de supply-chain (blocking-human).</action>
  <what-built>Ainda nada instalado. Esta é a barreira de supply-chain (D-14 / ameaça T-01-01-SC): a RESEARCH marcou 7 pacotes como [SUS] pelo heurístico "too-new" (release recente de projetos ativos, não slopsquat). O protocolo de legitimidade exige verificação humana do nome exato e da versão antes da primeira instalação.</what-built>
  <how-to-verify>
    Confirme cada pacote no npmjs.com (nome EXATO, repo oficial, downloads plausíveis) e anote a versão `latest` no momento da execução:
    1. `tsdown` → npmjs.com/package/tsdown (repo github.com/rolldown/tsdown)
    2. `vitest` → npmjs.com/package/vitest (repo github.com/vitest-dev/vitest)
    3. `@testcontainers/mongodb` → npmjs.com/package/@testcontainers/mongodb (repo github.com/testcontainers/testcontainers-node)
    4. `testcontainers` → npmjs.com/package/testcontainers (mesmo repo)
    5. `@arethetypeswrong/cli` → npmjs.com/package/@arethetypeswrong/cli
    6. `tsx` → npmjs.com/package/tsx (repo github.com/privatenumber/tsx)
    7. `@vitest/coverage-v8` → npmjs.com/package/@vitest/coverage-v8 (mesmo repo do vitest, mesma versão major do vitest)
    `publint`, `tsup` e `vite-tsconfig-paths` já foram VERIFIED na pesquisa — não precisam de recheck.
    Nota: `@vitest/coverage-v8` deve casar a major do `vitest` instalado (evitar peer mismatch).
  </how-to-verify>
  <resume-signal>Digite "approved" para prosseguir com a instalação, ou liste qualquer pacote/versão a ajustar.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Migrar package.json (engines/files/exports/scripts/deps) e tsconfig target</name>
  <files>package.json, tsconfig.json</files>
  <read_first>
    - package.json (estado atual — engines, files, exports, scripts, deps)
    - tsconfig.json (target atual ES2022, paths, useUnknownInCatchVariables)
    - .planning/phases/01-funda-o-core-sem-bugs-e-build-moderno/01-RESEARCH.md (seção "Code Examples" → exports map dual literal; seção "Installation" → lista exata de install/uninstall; seção "Standard Stack" → versões [ASSUMED])
    - .planning/phases/01-funda-o-core-sem-bugs-e-build-moderno/01-PATTERNS.md (seção package.json → alvo de exports/engines/files/scripts)
    - .planning/phases/01-funda-o-core-sem-bugs-e-build-moderno/01-CONTEXT.md (D-01..D-05, D-12, D-14, D-16 e "Claude's Discretion" sobre subpath exports)
  </read_first>
  <action>
    Instale (via `npm install --save-dev`) as versões aprovadas na Task 1: `tsdown`, `vitest`, `@vitest/coverage-v8`, `@testcontainers/mongodb`, `testcontainers`, `@arethetypeswrong/cli`, `publint`, `tsx`, `vite-tsconfig-paths`. Desinstale (`npm uninstall`) as devDependencies mortas: `ts-jest`, `ts-node-dev`, `tsc-alias`, `tsconfig-paths`, `typescript-cached-transpile` (D-12). Mova `json-schema` para devDependencies (`npm install json-schema --save-dev`) — QUAL-04: sai do runtime (a remoção total do import é decidida no plan 02 conforme resultado do attw). Mantenha `typescript` em 5.9.x (NÃO subir para 6.x). Depois edite `package.json` manualmente:
    - `engines.node` = `"^20.19.0 || >=22.12.0"` (D-01).
    - `files` = `["lib"]` apenas — remover `"src"` e `"tsconfig.json"` (D-05).
    - `main` = `"./lib/index.cjs"`, `module` = `"./lib/index.mjs"`, `types` = `"./lib/index.d.cts"`. Manter `"type": "commonjs"` (ou omitir); NUNCA `"type": "module"`.
    - `exports`: root-only dual. **Decisão (Claude's Discretion, D subpath):** REMOVER os subpath exports (`./database`, `./model`, `./utils`, `./types`) — o barrel raiz `src/index.ts` já re-exporta tudo, e mantê-los quadruplica a superfície do exports map em formato dual. Breaking em alpha é barato (princípio do usuário). O `.` deve ter `import` → `{ "types": "./lib/index.d.mts", "default": "./lib/index.mjs" }` e `require` → `{ "types": "./lib/index.d.cts", "default": "./lib/index.cjs" }`, com `types` como PRIMEIRA key de cada condition (senão TS não resolve sob moduleResolution nodenext).
    - `scripts`: `build` = `"tsdown"`; `check:package` = `"npm pack --dry-run && npx publint && npx @arethetypeswrong/cli --pack ."` (D-14); `test` = `"vitest run"`; `test:watch` = `"vitest"`; `typecheck` = `"tsc --noEmit"`; `example` = `"tsx"` (D-16); manter `prebuild` = `"rimraf ./lib"`.
    Em `tsconfig.json`: `target` = `"ES2023"` (D-02) e alinhar `lib` ao target (ex.: incluir `ES2023`). NÃO alterar `useUnknownInCatchVariables` (permanece `false` — Pitfall 4). O `tsconfig.build.json` deixa de ser usado pelo build (tsdown não depende dele); pode ser removido no plan 02.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const a=require('assert'); a.strictEqual(p.engines.node,'^20.19.0 || >=22.12.0'); a.deepStrictEqual(p.files,['lib']); a.ok(!p.dependencies['json-schema'],'json-schema still in dependencies'); a.ok(!p.devDependencies['ts-jest']&&!p.devDependencies['tsc-alias']&&!p.devDependencies['tsconfig-paths']&&!p.devDependencies['ts-node-dev']&&!p.devDependencies['typescript-cached-transpile'],'dead dep remains'); a.strictEqual(p.exports['.'].import.default,'./lib/index.mjs'); a.strictEqual(p.exports['.'].require.default,'./lib/index.cjs'); a.strictEqual(Object.keys(p.exports['.'].import)[0],'types'); a.ok(!p.exports['./model'],'subpath export not removed'); a.ok(p.scripts['check:package']&&p.scripts.build==='tsdown'&&p.scripts.test==='vitest run'); a.ok(p.devDependencies.typescript.startsWith('5.9')); console.log('package.json OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `npm pkg get engines.node` retorna `"^20.19.0 || >=22.12.0"`.
    - `npm pkg get files` retorna `["lib"]`.
    - `json-schema` ausente de `dependencies`; presente em `devDependencies` (ou removido).
    - `ts-jest`, `ts-node-dev`, `tsc-alias`, `tsconfig-paths`, `typescript-cached-transpile` ausentes de `devDependencies`.
    - `exports["."]` tem apenas `import`/`require`, cada um com `types` como primeira chave; subpath exports (`./model` etc.) ausentes.
    - `scripts.build === "tsdown"`, `scripts["check:package"]`, `scripts.test`, `scripts["test:watch"]`, `scripts.typecheck`, `scripts.example` presentes.
    - `tsconfig.json` contém `"target": "ES2023"` e `useUnknownInCatchVariables` permanece `false`.
    - `devDependencies.typescript` começa com `5.9`.
  </acceptance_criteria>
  <done>Manifesto e tsconfig migrados; tooling instalado; deps mortas e json-schema fora do runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Criar a classe base MongoatError e exportá-la no barrel</name>
  <files>src/errors/index.ts, src/index.ts</files>
  <read_first>
    - src/index.ts (padrão de barrel export — `export { X } from './Y';`)
    - src/database/index.ts (linhas 36-53 — convenção de JSDoc `@public`)
    - .planning/phases/01-funda-o-core-sem-bugs-e-build-moderno/01-RESEARCH.md (seção "Code Examples" → MongoatError base class; D-11)
    - .planning/phases/01-funda-o-core-sem-bugs-e-build-moderno/01-PATTERNS.md (seção src/errors/index.ts)
  </read_first>
  <behavior>
    - `new MongoatError('msg') instanceof MongoatError` é `true`.
    - `new MongoatError('msg') instanceof Error` é `true`.
    - `new MongoatError('msg').name` é `'MongoatError'`.
    - `new MongoatError('msg', { cause: err }).cause` preserva `err` (referência original).
    - `import { MongoatError } from '@/errors'` resolve; `MongoatError` também é reexportado por `src/index.ts` (barrel raiz).
  </behavior>
  <action>
    Crie `src/errors/index.ts` com a classe `MongoatError` que estende `Error`, recebe `(message: string, options?: { cause?: unknown })`, chama `super(message, options)`, seta `this.name = 'MongoatError'` e faz `Object.setPrototypeOf(this, MongoatError.prototype)` (para `instanceof` funcionar sob target ES2023). Adicione JSDoc `@public` acima da classe seguindo a convenção de `src/database/index.ts`. Registre o export no barrel raiz `src/index.ts` (`export { MongoatError } from './errors';`), agrupado com os demais re-exports. Não crie subclasses ainda — a hierarquia completa de erros é escopo da Fase 3 (D-11 / Deferred).
  </action>
  <verify>
    <automated>grep -q "class MongoatError extends Error" src/errors/index.ts && grep -q "from './errors'" src/index.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/errors/index.ts` contém `class MongoatError extends Error` com `this.name = 'MongoatError'` e `Object.setPrototypeOf`.
    - `src/index.ts` reexporta `MongoatError` de `./errors`.
    - `npx tsc --noEmit` passa (0 erros).
    - Comportamento: `new MongoatError('x', { cause: new Error('y') }).cause.message === 'y'`.
  </acceptance_criteria>
  <done>MongoatError existe, é `instanceof Error`, preserva `cause` e está no barrel raiz.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev/CI machine | Pacotes de tooling baixados e executados durante build/test (entrada não confiável) |
| build output → npm consumers | O que `files` publica é o que consumidores instalam |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01-SC | Tampering | npm installs (tsdown, vitest, testcontainers, attw, tsx, publint, coverage-v8) | high | mitigate | Task 1: `checkpoint:human-verify` blocking-human antes da instalação; verificar nome/versão/repo em npmjs.com; pinar versão aprovada |
| T-01-01-02 | Information Disclosure | `files` do package.json | medium | mitigate | `files: ["lib"]` (D-05) remove `src`/`tsconfig.json` do tarball publicado — reduz superfície e vazamento de fonte |
| T-01-01-03 | Tampering | campo `type`/`exports` do package.json | low | accept | Manter `"type": "commonjs"` + extensões explícitas `.mjs`/`.cjs` evita dual-package hazard; validação completa é do plan 02 (attw/publint) |
</threat_model>

<verification>
- `node -e` de asserção do package.json passa (ver Task 2).
- `npx tsc --noEmit` passa após criar MongoatError.
- `npm ls ts-jest` não lista o pacote (dep morta removida).
- Checkpoint de legitimidade aprovado antes de qualquer install.
</verification>

<success_criteria>
- package.json declara engines Node >=20.19, files=["lib"], exports dual root-only, scripts modernos; json-schema e deps mortas fora do runtime/devDeps.
- tsconfig target ES2023; useUnknownInCatchVariables intacto.
- MongoatError disponível em `@/errors` e no barrel raiz, `instanceof Error`, com `cause`.
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-core-sem-bugs-e-build-moderno/01-01-SUMMARY.md` when done.
</output>
