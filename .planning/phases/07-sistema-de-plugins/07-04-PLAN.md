---
phase: 07-sistema-de-plugins
plan: 04
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - test/model/plugins-static-binding.test.ts
  - examples/plugins/paginate-plugin.ts
  - examples/plugins/timestamps-plugin.ts
  - examples/plugins/augmentation.ts
autonomous: true
requirements: [PLUG-01]
must_haves:
  truths:
    - "Um static de plugin chamado via a instância Proxy-wrapped tem `this` bound ao model — `this.getCollection()`/`this.find()` funcionam de dentro do static, contra MongoDB real (D-12)."
    - "Um plugin parametrizável é composição pura de JS/TS (factory que recebe opções e devolve o plugin) — zero API extra no core (D-02)."
    - "A tipagem de statics de plugin é resolvida por module augmentation (`declare module`), NÃO por inferência via construtor — `.paginate()` fica tipado sem o consumidor anotar nada no call-site (D-09b)."
  artifacts:
    - test/model/plugins-static-binding.test.ts
    - examples/plugins/paginate-plugin.ts
    - examples/plugins/timestamps-plugin.ts
    - examples/plugins/augmentation.ts
  key_links:
    - "O bind vem do Proxy trap `value.bind(target)` (`src/database/index.ts:357-358`) — nenhum `.bind` manual; o teste de integração prova via testcontainers."
    - "O exemplo `declare module` augmenta a MESMA especificação de módulo por onde `Model` é importado (`@/model`) — declaration merging class+interface do TS."
    - "`npx tsc --noEmit` (que inclui `examples/`) é a verificação automatizada da camada de tipos do D-09b."
---

<objective>
Provar e documentar as duas pontas que faltam: (1) o bind de statics de plugin via o retorno Proxy do construtor (D-12) — verificado por teste de integração com MongoDB real (testcontainers), onde um static `paginate` usa `this.getCollection()`; e (2) o veredito de tipagem D-09b — a inferência-plena via `new Model({ plugins })` foi provada NÃO viável (TS1093 + tipo de `new` fixo), então a forma oficial de tipar statics é module augmentation (`declare module` augmentando a interface `Model`), padrão consagrado (Fastify). O exemplo também materializa o factory pattern (D-02) para plugins parametrizáveis e o "selo por semver do pacote" (D-15) — sem `apiVersion`.

Purpose: Fechar a camada de integração (bind real) e a camada de tipos (augmentation) da fase, entregando exemplos executáveis/type-checkáveis que servirão de base ao guia de plugins (DOCS de fase futura).

Output: `test/model/plugins-static-binding.test.ts` verde; exemplos em `examples/plugins/` type-check com `npx tsc --noEmit`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sistema-de-plugins/07-CONTEXT.md
@.planning/phases/07-sistema-de-plugins/07-RESEARCH.md
@.planning/phases/07-sistema-de-plugins/07-VALIDATION.md
@src/database/index.ts
@src/model/index.ts
@examples/model/model.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Teste de integração — bind de static de plugin via Proxy (D-12)</name>
  <files>test/model/plugins-static-binding.test.ts</files>
  <read_first>
    - src/database/index.ts linhas 337-363 (Proxy trap `value.bind(target)` — a fonte do bind automático)
    - src/model/index.ts (escape hatch `getCollection`; construtor com `applyPlugins` do Plano 02)
    - src/model/plugins.ts (`registerPluginStatic` — como o static é anexado a `this`)
    - test/setup/testcontainer.ts (globalSetup do container `mongo:7`; env `MONGODB_URI`/`MONGODB_DB_NAME`)
    - test/model/hooks-pre-order.test.ts (padrão `beforeAll`/`afterAll` com `db.connect()`/`db.disconnect()` + `Database.resetRegistry()`)
    - .planning/phases/07-sistema-de-plugins/07-VALIDATION.md (linha D-12 — integração real)
  </read_first>
  <action>
    Criar `test/model/plugins-static-binding.test.ts` (integração, testcontainers): definir um plugin cujo `setup` chama `ctx.static('paginate', ...)` onde a função usa `this.getCollection()` (e/ou `this.find()`) para paginar de fato; construir `new Model({ plugins: [esse] })` conectado ao MongoDB real; inserir alguns documentos; chamar `model.paginate(...)` através da instância Proxy-wrapped e asserir que retorna os documentos esperados — provando que `this` está bound ao model (o static acessa a superfície pública sem `this` undefined). Usar `beforeAll` com `db.connect()`, `afterAll` com `Database.resetRegistry()` + (se globais forem usados) `Model[kResetPlugins]()` + `db.disconnect()`, seguindo o analog `hooks-pre-order.test.ts`. Nenhuma mudança de código de produção — o bind já vem do Proxy trap (D-12).
  </action>
  <verify>
    <automated>npx vitest run test/model/plugins-static-binding.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test/model/plugins-static-binding.test.ts` define um static que chama `this.getCollection()` dentro do `setup` do plugin
    - o teste insere e recupera documentos reais via `model.paginate(...)` (MongoDB via testcontainers)
    - o teste assere o resultado paginado esperado (prova de `this` bound)
    - `npx vitest run test/model/plugins-static-binding.test.ts` exits 0
  </acceptance_criteria>
  <done>Bind de statics de plugin (D-12) provado contra MongoDB real, sem nenhum `.bind` manual — só o Proxy trap existente.</done>
</task>

<task type="auto">
  <name>Task 2: Exemplos — factory pattern (D-02) + module augmentation D-09b (tipagem de statics)</name>
  <files>examples/plugins/paginate-plugin.ts, examples/plugins/timestamps-plugin.ts, examples/plugins/augmentation.ts</files>
  <read_first>
    - examples/model/model.ts (convenção de import via aliases `@/model`, `@/types`, `@/utils/enums`; estilo dos exemplos e JSDoc `@see`)
    - src/types/plugin.ts (tipos `Plugin`/`PluginContext` a usar nos exemplos)
    - src/model/index.ts (classe `Model` exportada — alvo do `declare module` augmentation)
    - .planning/phases/07-sistema-de-plugins/07-RESEARCH.md § D-09 Veredito (Achado 5 — module augmentation é o padrão; Achado 1/2 — por que a inferência via `new` não funciona) e § Code Examples (factory `timestamps`)
    - tsconfig.json (confirma que `examples` está no `include` — `npx tsc --noEmit` type-checka os exemplos)
  </read_first>
  <action>
    Criar `examples/plugins/timestamps-plugin.ts`: um factory `timestamps(options?: { createdField?: string; updatedField?: string })` que devolve um `PluginObject` cujo `setup` registra `ctx.pre(METHODS.INSERT, ...)`/`ctx.pre(METHODS.UPDATE, ...)` — demonstrando D-02 (composição pura, o autor tipa suas próprias opções, zero API extra no core).
    Criar `examples/plugins/paginate-plugin.ts`: um plugin que registra um static `paginate(page, pageSize)` usando `this.getCollection()` (o autor do plugin declara o SHAPE do static para documentação).
    Criar `examples/plugins/augmentation.ts`: importar `Model` de `@/model`, aplicar o `paginate` via `new Model({ plugins: [paginatePlugin] })`, e usar um bloco `declare module '@/model' { interface Model<ModelType extends Document> { paginate(page: number, pageSize: number): Promise<...>; } }` para tipar `.paginate()` — chamando `model.paginate(1, 20)` SEM cast e SEM `any` no call-site (prova de que a augmentation, não a inferência, é o que tipa). Incluir um comentário `//` explicando que a inferência via construtor é inviável (veredito D-09) e que esta é a forma oficial (D-09b), e que o "selo" do contrato é a estabilidade do tipo sob semver do pacote (D-15), sem `apiVersion`. NÃO usar JSDoc `@public` que cite IDs de planejamento.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `examples/plugins/augmentation.ts` contém `declare module` augmentando `interface Model`
    - `examples/plugins/augmentation.ts` chama `.paginate(` sem `as`/`any` no call-site
    - `examples/plugins/timestamps-plugin.ts` é um factory que recebe opções e devolve um plugin (D-02)
    - `examples/plugins/paginate-plugin.ts` registra um static via `ctx.static(` usando `this.getCollection()`
    - `npx tsc --noEmit` exits 0 (type-checa `examples/`, resolvendo `.paginate()` via augmentation)
  </acceptance_criteria>
  <done>Factory pattern (D-02) e a forma oficial de tipagem de statics via module augmentation (D-09b) demonstrados em exemplos que type-check limpo; inferência via construtor documentada como inviável.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plano 04)

- `test/model/plugins-static-binding.test.ts` (NOVO) — D-12 (bind de static via Proxy, integração real com MongoDB)
- `examples/plugins/timestamps-plugin.ts` (NOVO) — D-02 (factory parametrizável)
- `examples/plugins/paginate-plugin.ts` (NOVO) — static usando `this.getCollection()` (D-12)
- `examples/plugins/augmentation.ts` (NOVO) — D-09b (module augmentation tipando `.paginate()`) + nota D-15 (selo por semver, sem `apiVersion`)

Nenhum código de produção novo (bind já herdado do Proxy trap; tipagem via augmentation do consumidor). Nenhuma dependência nova (D-02).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static de plugin → driver nativo | O static usa o escape hatch (`this.getCollection()`) para tocar o driver cru; é a MESMA superfície pública já auditada na Fase 2, sem nova via de bypass. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02 | Elevation of Privilege | static de plugin de terceiro acessando o driver via `this.getCollection()` | high | accept | O escape hatch é bypass DELIBERADO e documentado desde a Fase 2; um static de plugin tem exatamente a mesma superfície de um método manual do model — nenhuma escalada nova. Risco é o CÓDIGO do plugin (supply-chain in-process), não uma brecha do core; disciplina de auditoria de dependências documentada. |
| T-07-07 | Tampering (type-safety) | consumidor tipando statics via `any`/cast em vez de augmentation | low | mitigate | Exemplo canônico `declare module` (D-09b) demonstra a forma correta sem `any`/cast; `npx tsc --noEmit` sobre `examples/` guarda a regressão da camada de tipos. |
| T-07-SC | Tampering | instalação de pacotes (npm) | n/a | accept | Zero deps novas nesta fase (D-02) — nenhum `npm install` a auditar. |
</threat_model>

<verification>
- `npx vitest run test/model/plugins-static-binding.test.ts` verde (integração testcontainers).
- `npx tsc --noEmit` verde com `examples/plugins/` presente (camada de tipos D-09b).
- `npm test` (suíte completa) verde ao fim da wave.
</verification>

<success_criteria>
- Bind de static de plugin provado contra MongoDB real (D-12).
- Factory pattern parametrizável demonstrado sem API extra no core (D-02).
- Tipagem de statics via module augmentation type-checando limpo, sem anotação no call-site (D-09b); inferência via construtor documentada como inviável.
</success_criteria>

<output>
Create `.planning/phases/07-sistema-de-plugins/07-04-SUMMARY.md` when done
</output>
