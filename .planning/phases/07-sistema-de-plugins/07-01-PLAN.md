---
phase: 07-sistema-de-plugins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/plugin.ts
  - src/types/model.ts
  - src/types/index.ts
  - src/index.ts
  - src/errors/index.ts
  - src/model/plugins.ts
  - test/model/plugins-resolve.test.ts
autonomous: true
requirements: [PLUG-01, PLUG-03]
must_haves:
  truths:
    - "O tipo `Plugin<ModelType>` aceita função OU objeto `{ name, setup }` e o consumidor importa `Plugin`/`PluginContext` do barrel público (D-01)."
    - "`resolvePluginList` aplica o mesmo plugin (mesma referência) 1x e lança `DUPLICATE_PLUGIN_NAME` para nomes iguais com refs diferentes (D-07)."
    - "`registerPluginStatic` lança `STATIC_COLLISION` contra o conjunto COMPLETO de nomes reservados — incluindo privados de runtime como `rawInsert` (D-08)."
    - "`buildPluginContext` expõe `schema` via `structuredClone` e `allowedMethods` congelado — mutar a cópia nunca alcança a referência viva (D-03)."
    - "`applyPlugins` roda cada `setup()` de forma síncrona em try/catch e envolve qualquer falha em `PLUGIN_SETUP_FAILED` com `.cause` preservado (D-04/D-10)."
  artifacts:
    - src/types/plugin.ts
    - src/model/plugins.ts
    - test/model/plugins-resolve.test.ts
  key_links:
    - "`buildPluginContext`/`registerPluginStatic`/`applyPlugins` operam sobre um alvo ESTRUTURAL (não importam `Model`) — sem ciclo de módulos; o construtor (Plano 02) passa `this`."
    - "`structuredClone` do schema é o mesmo idioma de `schemaValidatorBuilder`/`cloneDocumentDefaults` — nunca `Object.freeze` da referência viva (D-03)."
    - "`RESERVED_NAMES` enumera os nomes reais do `Model.prototype` (RESEARCH § Pattern 4), não só `Object.values(METHODS)`."
---

<objective>
Estabelecer a fundação de tipos e a lógica pura de resolução de plugins, SEM ainda tocar no construtor do `Model`. Entrega o tipo público `Plugin<ModelType>`/`PluginContext<ModelType>` (D-01/D-03), o campo `plugins?` em `CreateModelProps` (PLUG-01), e o módulo `src/model/plugins.ts` com toda a mecânica testável em isolamento: normalização (D-01), dedup por referência + colisão de nome (D-07), guarda de colisão de statics contra o conjunto completo de nomes reservados (D-08), selo read-only via `structuredClone` (D-03) e o orquestrador `applyPlugins` com fail-loud `PLUGIN_SETUP_FAILED` (D-04/D-10).

Purpose: Isolar a lógica pura (sem I/O, sem `Model` vivo, sem DB) para que dedup/colisão/selo/fail-loud sejam verificados por teste unitário verde já na Wave 1 — e para que o Plano 02 apenas conecte um único ponto no construtor.

Output: `src/types/plugin.ts`, `src/model/plugins.ts`, campo `plugins?` em `CreateModelProps`, exports de barrel, JSDoc de erros atualizado, e `test/model/plugins-resolve.test.ts` verde.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sistema-de-plugins/07-CONTEXT.md
@.planning/phases/07-sistema-de-plugins/07-RESEARCH.md
@.planning/phases/07-sistema-de-plugins/07-PATTERNS.md
@.planning/phases/07-sistema-de-plugins/07-VALIDATION.md
@src/types/hooks.ts
@src/types/model.ts
@src/errors/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipos de plugin + campo `plugins?` + exports + JSDoc de erros</name>
  <files>src/types/plugin.ts, src/types/model.ts, src/types/index.ts, src/index.ts, src/errors/index.ts</files>
  <read_first>
    - src/types/hooks.ts (moldes `HookFn`, `HookConfig`, `HookContextMap`, `PostHookEntry` — a superfície de tipo a compor)
    - src/types/model.ts (campo `hooks?:` existente na `CreateModelProps` — analog EXATO do novo campo `plugins?`; e `ModelValidationSchema`)
    - src/types/index.ts + src/index.ts (barrels — onde novos tipos são re-exportados)
    - src/errors/index.ts (JSDoc `@public` da `MongoatValidationError`, linhas 44-50 — lista de codes em prosa a atualizar)
    - .planning/phases/07-sistema-de-plugins/07-RESEARCH.md § Code Examples (bloco `src/types/plugin.ts` proposto, linhas 460-509) e 07-PATTERNS.md § `src/types/plugin.ts`
  </read_first>
  <action>
    Criar `src/types/plugin.ts` exportando: `PluginContext<ModelType extends Document = Document>` (interface com `readonly collectionName: string`, `readonly allowedMethods: readonly METHODS[]`, `readonly schema: Readonly<ModelValidationSchema>`, mais os métodos de registro `pre<M extends METHODS>(method, fn: HookFn<HookContextMap<ModelType>[M]>): void`, `post<M extends METHODS>(method, fn, options?: { fireAndForget?: boolean }): void`, `static(name: string, fn: (...args: never[]) => unknown): void`), `PluginSetup<ModelType>` (`(ctx) => void`), `PluginObject<ModelType>` (`{ name?: string; setup: PluginSetup<ModelType> }`) e `Plugin<ModelType> = PluginSetup<ModelType> | PluginObject<ModelType>` (D-01/D-03). Importar `Document` de `mongodb`, `HookContextMap`/`HookFn` de `@/types/hooks`, `METHODS` de `@/utils/enums`, `ModelValidationSchema` de `@/types/model`. NÃO declarar nenhum acumulador de tipos (`MergeStatics`) no core — veredito D-09 proíbe (código morto do ponto de vista do consumidor); a tipagem de statics é resolvida por module augmentation no Plano 04.
    Em `src/types/model.ts`: adicionar `plugins?: Plugin<ModelType>[]` à interface `CreateModelProps<ModelType>`, com JSDoc no MESMO estilo do campo `hooks?:` explicando ordem/precedência ("aplicados DEPOIS dos globais `Model.plugin()` e ANTES do wrap em Proxy"). Importar `Plugin` de `@/types/plugin`. O JSDoc é público (vaza p/ TypeDoc) — NÃO citar IDs de planejamento (D-0x/PLUG-0x/Fase X) nem símbolos privados no texto do JSDoc; descrever por conceito.
    Re-exportar `Plugin`, `PluginObject`, `PluginSetup`, `PluginContext` como `export type` em `src/types/index.ts` e em `src/index.ts` (junto dos demais tipos públicos).
    Em `src/errors/index.ts`: acrescentar `DUPLICATE_PLUGIN_NAME`, `STATIC_COLLISION`, `PLUGIN_SETUP_FAILED` e `PLUGIN_REGISTERED_TOO_LATE` à lista de codes em prosa do JSDoc `@public` da `MongoatValidationError` (nenhuma subclasse nova — os codes são apenas strings do `code` já existente). Nenhuma mudança de código executável em errors.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `src/types/plugin.ts` contains `export interface PluginContext`
    - `src/types/plugin.ts` contains `export type Plugin`
    - `src/types/model.ts` contains `plugins?: Plugin`
    - `src/index.ts` re-exports `Plugin` and `PluginContext` (grep de `Plugin` no bloco `export type`)
    - `src/errors/index.ts` JSDoc menciona `PLUGIN_SETUP_FAILED`
  </acceptance_criteria>
  <done>Tipos públicos de plugin existem e compilam; `CreateModelProps` aceita `plugins?`; barrels e JSDoc de erros atualizados; zero código executável novo em errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Módulo puro `src/model/plugins.ts` (normalize, dedup, colisão, selo, applyPlugins)</name>
  <files>src/model/plugins.ts, test/model/plugins-resolve.test.ts</files>
  <read_first>
    - src/model/hooks.ts (padrão de função pura recebendo registry/ctx e produzindo efeito — analog de role)
    - src/model/index.ts linhas 314-316 (formato do `hooks` registry `{ pre: []; post: [] }` por método — o alvo dos `ctx.pre`/`ctx.post`) e linhas 532-536 (normalização inline `typeof entry === 'function' ? { fn: entry } : entry`, molde de `normalizePlugin`) e linhas 562-569 (`structuredClone` idiomático)
    - src/database/index.ts linhas 337-363 (Proxy trap `value.bind(target)` — CONFIRMA que statics anexados a `this` NÃO precisam `.bind()` manual, D-12)
    - src/utils/enums.ts (valores de `METHODS` para `RESERVED_NAMES`)
    - .planning/phases/07-sistema-de-plugins/07-RESEARCH.md § Architecture Patterns (Pattern 1/2/3/4, incluindo a lista COMPLETA de `RESERVED_NAMES` com os privados `rawInsert`/`executeHooked`/etc.) e § Common Pitfalls (Pitfall 2/3)
    - src/errors/index.ts (`MongoatValidationError` — forma `new MongoatValidationError(msg, { cause?, code })`)
  </read_first>
  <behavior>
    - normalizePlugin(fn) → `{ name: fn.name || '<anonymous>', setup: fn }`; normalizePlugin(obj) → devolve o próprio objeto (D-01).
    - resolvePluginList(global, local): concatena global-primeiro; mesma referência repetida (global+local, ou 2x no mesmo array) é aplicada 1x (dedup por referência ORIGINAL, não pelo objeto normalizado) — D-07.
    - resolvePluginList: dois plugins com o MESMO `name` mas referências DIFERENTES lançam `MongoatValidationError` code `DUPLICATE_PLUGIN_NAME`.
    - registerPluginStatic(target, name, fn, pluginName, owners): `name` em `RESERVED_NAMES` (ex.: `find`, `getCollection`, `rawInsert`) lança `STATIC_COLLISION`; `name` já registrado por OUTRO plugin lança `STATIC_COLLISION` citando o dono anterior; caso ok, anexa `fn` como propriedade de `target` (sem `.bind` — D-12) e grava o dono em `owners`.
    - buildPluginContext(target, pluginName, owners): `ctx.allowedMethods` é `Object.freeze([...target.allowedMethods])`; `ctx.schema` é `structuredClone(target.validator.$jsonSchema)` — mutar `ctx.schema.properties.<x>` NUNCA altera `target.validator.$jsonSchema`; `ctx.pre`/`ctx.post` empurram para `target.hooks[method].pre/post` (post normaliza para `{ fn, fireAndForget }`); `ctx.static` delega a registerPluginStatic.
    - applyPlugins(target, global, local): resolve a lista, cria um `owners` Map compartilhado, e para cada plugin único (ordem global→local) chama `setup(ctx)` SINCRONAMENTE dentro de try/catch; erro → `MongoatValidationError` code `PLUGIN_SETUP_FAILED`, mensagem inclui o `name` do plugin culpado, `.cause` = erro original; aborta imediatamente (não aplica os plugins seguintes).
  </behavior>
  <action>
    Criar `src/model/plugins.ts` com as funções puras acima. Definir um tipo estrutural interno `PluginTarget` (ex.: `{ collectionName: string; allowedMethods: METHODS[]; validator: { $jsonSchema: ModelValidationSchema }; hooks: HookRegistry<Document> }` com index signature para os statics) para que o módulo NÃO importe `Model` (evita ciclo — mesmo padrão unidirecional de `@/model/hooks`). `RESERVED_NAMES` é um `Set<string>` construído a partir de `Object.values(METHODS)` MAIS os nomes públicos (`getCollection`, `pre`, `post`, `collectionName`, `indexes`, `validator`, `validationAction`, `validationLevel`, `methods`, `allowedMethods`, `documentDefaults`, `hooks`, `onHookError`, `schemaClass`) e os privados de runtime enumerados no RESEARCH § Pattern 4 (`schemaValidatorBuilder`, `includeAdditionalPropertiesFalse`, `getCollectionOrThrow`, `buildClassDefaults`, `executeHooked`, `runHooked`, `rawAggregate`, `rawUpdate`, `rawUpdateMany`, `rawFindMany`, `rawDeleteMany`, `rawInsert`, `rawInsertMany`, `rawFind`, `rawFindById`, `rawDelete`, `rawTotal`, `rawBulkWrite`). Dedup por referência usa um `Set`/`Map` chaveado pela referência ORIGINAL (não pelo objeto normalizado recém-criado). Erros usam `MongoatValidationError` com os codes exatos — nunca subclasse nova. `applyPlugins` é `export`ado para consumo pelo construtor no Plano 02; `resolvePluginList`, `registerPluginStatic`, `buildPluginContext`, `normalizePlugin` também exportados para teste direto.
    Criar `test/model/plugins-resolve.test.ts` cobrindo cada bullet do bloco behavior chamando as funções puras com um `PluginTarget` FALSO em memória (sem `Database`, sem testcontainers) — inclusive o caso de colisão contra o privado `rawInsert` e o caso de mutar `ctx.schema` e reconferir que o alvo não mudou.
  </action>
  <verify>
    <automated>npx vitest run test/model/plugins-resolve.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/model/plugins.ts` contains `export function applyPlugins`
    - `src/model/plugins.ts` contains `structuredClone(`
    - `src/model/plugins.ts` references `'rawInsert'` dentro do conjunto de nomes reservados
    - `npx vitest run test/model/plugins-resolve.test.ts` exits 0
    - o teste assere `MongoatValidationError` com `.code === 'DUPLICATE_PLUGIN_NAME'`, `'STATIC_COLLISION'` e `'PLUGIN_SETUP_FAILED'`
    - o teste assere que mutar `ctx.schema` não altera o `validator.$jsonSchema` do alvo
  </acceptance_criteria>
  <done>Módulo puro completo e verde por teste unitário sem DB; dedup, colisão contra nomes reservados (incl. privados), selo via structuredClone e fail-loud provados em isolamento.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plano 01)

Símbolos/paths criados ou modificados neste plano:
- `src/types/plugin.ts` (NOVO) — tipos `Plugin<ModelType>`, `PluginObject<ModelType>`, `PluginSetup<ModelType>`, `PluginContext<ModelType>`
- `src/types/model.ts` — campo `plugins?: Plugin<ModelType>[]` em `CreateModelProps`
- `src/types/index.ts`, `src/index.ts` — re-export público de `Plugin`/`PluginObject`/`PluginSetup`/`PluginContext`
- `src/errors/index.ts` — JSDoc de codes atualizado (`DUPLICATE_PLUGIN_NAME`, `STATIC_COLLISION`, `PLUGIN_SETUP_FAILED`, `PLUGIN_REGISTERED_TOO_LATE`); zero código novo
- `src/model/plugins.ts` (NOVO) — `normalizePlugin`, `resolvePluginList`, `RESERVED_NAMES`, `registerPluginStatic`, `buildPluginContext`, `applyPlugins`
- `test/model/plugins-resolve.test.ts` (NOVO) — cobertura unitária pura (função-nível) de D-01/D-03/D-07/D-08/D-10

Codes de erro introduzidos (strings do `code` da `MongoatValidationError`): `DUPLICATE_PLUGIN_NAME`, `STATIC_COLLISION`, `PLUGIN_SETUP_FAILED`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autor-do-plugin → core Mongoat | O código de `setup()` de um plugin (potencialmente de terceiro) roda in-process; o core define a superfície de efeito permitida (`pre`/`post`/`static`) e o selo read-only. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | `registerPluginStatic` / `RESERVED_NAMES` (`src/model/plugins.ts`) | high | mitigate | Static colidindo com QUALQUER membro do `Model.prototype` — incluindo privados de runtime (`rawInsert`, `executeHooked`) — lança `STATIC_COLLISION` na resolução; nunca sobrescreve silenciosamente. Coberto por `test/model/plugins-resolve.test.ts` (colisão contra `rawInsert`). |
| T-07-03 | Tampering/Info Disclosure | `buildPluginContext.schema` (`src/model/plugins.ts`) | high | mitigate | `ctx.schema` é `structuredClone` da referência viva — mutar a cópia nunca alcança `validator.$jsonSchema`; contrato de validação server-side permanece imutável a partir do plugin (D-03). Coberto por assert de imutabilidade em `plugins-resolve.test.ts`. |
| T-07-SC | Tampering | instalação de pacotes (npm) | n/a | accept | Nenhum pacote novo é instalado nesta fase (D-02, zero deps novas) — sem `npm install` a auditar; sem checkpoint de legitimidade necessário. |
</threat_model>

<verification>
- `npx tsc --noEmit` verde (tipos públicos + campo `plugins?`).
- `npx vitest run test/model/plugins-resolve.test.ts` verde (lógica pura: dedup, colisão, selo, fail-loud).
- Nenhuma dependência nova em `package.json` (D-02).
</verification>

<success_criteria>
- Tipo `Plugin<ModelType>`/`PluginContext<ModelType>` exportado do barrel público e compilando.
- `CreateModelProps` aceita `plugins?: Plugin<ModelType>[]`.
- `src/model/plugins.ts` entrega toda a mecânica pura (D-01/D-03/D-07/D-08/D-10) verificada por teste unitário sem DB.
- Módulo não importa `Model` (sem ciclo).
</success_criteria>

<output>
Create `.planning/phases/07-sistema-de-plugins/07-01-SUMMARY.md` when done
</output>
