---
phase: 04-site-de-documenta-o
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - docs/migration.md
autonomous: true
requirements: [DOCS-03]
must_haves:
  truths:
    - "docs/migration.md consolida CHANGELOG.md + MIGRATION.md numa página do site, linkada em /migration na nav"
    - "Documenta TODAS as mudanças de API alpha→v1.0 (hooks, erros, input validation, env/build), cada uma com before/after/how-to-migrate"
  artifacts:
    - path: "docs/migration.md"
      provides: "Guia de migração alpha→v1.0 consolidado (fora dos 4 quadrantes)"
      min_lines: 60
  key_links:
    - from: "docs/migration.md"
      to: "MIGRATION.md + CHANGELOG.md (raiz)"
      via: "consolida os arquivos-fonte da raiz numa página do site (os arquivos raiz permanecem fonte editável)"
      pattern: "BREAKING"
    - from: "docs/.vitepress/config.mts (nav /migration)"
      to: "docs/migration.md"
      via: "a entrada Migration da nav resolve para esta página"
      pattern: "migration"
---

<objective>
Preencher `docs/migration.md` — o guia de migração alpha→v1.0 (DOCS-03), fora dos 4 quadrantes Diátaxis, linkado como "Migration" na nav — consolidando `CHANGELOG.md` e `MIGRATION.md` (raiz) numa única página do site. Substitui o stub do Plano 01 por conteúdo real e completo, cobrindo cada mudança de API com before/after/how-to-migrate.

Purpose: Entregar DOCS-03 — o guia que audita e documenta todas as breaking changes antes de congelar a API na v1.0 (Fase 5). A fonte da verdade passa a ser o site; os arquivos raiz `CHANGELOG.md`/`MIGRATION.md` permanecem como fonte editável (Claude's Discretion).
Output: página de migração consolidada, completa, buildando verde e resolvendo em /migration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-site-de-documenta-o/04-CONTEXT.md
@.planning/phases/04-site-de-documenta-o/04-RESEARCH.md

# Fonte da verdade da migração (a consolidar)
@CHANGELOG.md
@MIGRATION.md
</context>

## Artifacts this phase produces (escopo deste plano)

- `docs/migration.md` — guia de migração alpha→v1.0 consolidado (DOCS-03)

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Consolidar migration.md (alpha → v1.0)</name>
  <read_first>
    - MIGRATION.md (raiz — estrutura por seção: Hooks, Errors, Input validation, Environment & build; cada entrada tem what changed / before / after / how to migrate)
    - CHANGELOG.md (raiz — seções Added/Changed/Removed/Fixed/Security da [Unreleased] com os bullets BREAKING)
    - .planning/phases/04-site-de-documenta-o/04-CONTEXT.md §D-03 (fonte da verdade é o site) e §"Claude's Discretion" (consolidar CHANGELOG/MIGRATION; arquivos raiz permanecem editáveis)
  </read_first>
  <files>docs/migration.md</files>
  <action>
    Escrever `docs/migration.md` consolidando `MIGRATION.md` + `CHANGELOG.md` (raiz) numa página do site (idioma inglês). Preservar a estrutura por seção do `MIGRATION.md` (cada mudança com **what changed** / **before** / **after** / **how to migrate**) e garantir cobertura de TODAS as breaking changes do `CHANGELOG.md [Unreleased]`:
    - Hooks: (1) acumulam em vez de substituir; (2) assinatura `ctx` explícita (não `this`-bound); (3) post-hooks + `fireAndForget` (não-breaking, mas documentar).
    - Errors: (2.1) hierarquia tipada `MongoatError`/subclasses com `.code`/`.cause` (substitui `MongoError` + `JSON.stringify`); (2.2) métodos de leitura rejeitam a Promise quando desconectados (não lançam síncrono).
    - Input validation: (3.1) `toObjectId`/`findById` lançam em input inválido (`INVALID_OBJECT_ID`); (3.2) `$where` rejeitado incondicionalmente + `sanitizeFilter` opt-in.
    - Environment & build: (4.1) Node mínimo `^20.19.0 || >=22.12.0`; (4.2) subpath exports removidos (importar tudo da raiz); (4.3) `json-schema` runtime removido (não-breaking).

    Incluir uma nota no topo de que a página é a versão consolidada/publicada e que `CHANGELOG.md`/`MIGRATION.md` na raiz permanecem como fonte editável. Manter o status "living document até o v1.0.0-rc" coerente com o MIGRATION.md. Adicionar um índice (table of contents) no topo.

    Não introduzir links markdown para páginas stub inexistentes; cross-links permitidos apenas para páginas já na nav (how-to/handle-errors, how-to/sanitize-filters, Reference).
  </action>
  <verify>
    <automated>npm run docs:build && node -e "const fs=require('fs');const s=fs.readFileSync('docs/migration.md','utf8');['Hooks','Errors','sanitizeFilter','20.19','subpath'].forEach(t=>{if(!s.includes(t))throw new Error('migration.md faltando cobertura: '+t)});"</automated>
    <human-check>Preview: a entrada "Migration" da nav resolve para a página; todas as 4 seções (Hooks/Errors/Input validation/Environment) presentes com before/after.</human-check>
  </verify>
  <acceptance_criteria>
    - `docs/migration.md` cobre as 4 seções e TODAS as breaking changes do CHANGELOG [Unreleased], cada mudança com before/after/how-to-migrate.
    - Nota de topo sobre consolidação (fonte = site; arquivos raiz permanecem editáveis) + ToC.
    - A nav "Migration" (/migration) resolve para a página.
    - `npm run docs:build` verde.
  </acceptance_criteria>
  <done>Guia de migração alpha→v1.0 consolidado, completo e buildando (DOCS-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CHANGELOG.md/MIGRATION.md (raiz) → docs/migration.md | Conteúdo consolidado de arquivos-fonte versionados; sem input runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Tampering | Drift entre migration.md e os arquivos raiz | low | accept | D-03: a fonte da verdade é o site; os arquivos raiz permanecem editáveis. O drift é gerenciado editando ambos; risco baixo, sem impacto de segurança |
| T-04-11 | Information Disclosure | Exemplos before/after | low | mitigate | Exemplos derivados do MIGRATION.md existente — sem credenciais/dados reais |
</threat_model>

<verification>
- `npm run docs:build` verde.
- Cobertura das 4 seções + breaking changes verificada por grep automatizado.
- Revisão manual da página no `docs:preview` (nav /migration resolve).
</verification>

<success_criteria>
- DOCS-03 satisfeito: guia de migração consolidado, completo, linkado na nav, buildando.
- Sem tocar `config.mts`/`typedoc.json`/`package.json` (paralelismo Wave 2 preservado).
</success_criteria>

<output>
Create `.planning/phases/04-site-de-documenta-o/04-05-SUMMARY.md` when done
</output>
