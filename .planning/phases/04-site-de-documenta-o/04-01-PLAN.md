---
phase: 04-site-de-documenta-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
  - typedoc.json
  - docs/.vitepress/config.mts
  - docs/index.md
  - docs/tutorials/getting-started.md
  - docs/how-to/hooks.md
  - docs/how-to/sanitize-filters.md
  - docs/how-to/handle-errors.md
  - docs/how-to/escape-hatch.md
  - docs/how-to/indexes-validation.md
  - docs/explanation/thin-odm-philosophy.md
  - docs/explanation/proxy-gating.md
  - docs/explanation/server-side-validation.md
  - docs/migration.md
autonomous: true
requirements: [DOCS-01, DOCS-02]
must_haves:
  truths:
    - "npm run docs:build completes green (VitePress builda o site estático sem erro)"
    - "A Reference de API é gerada de src/index.ts e NÃO expõe membros protected/Symbol internos (kClient/kDb/etc.)"
    - "O site serve sob base '/mongoat/' com busca local (minisearch) habilitada e a nav dos 4 quadrantes Diátaxis + Home + Migration"
  artifacts:
    - path: "typedoc.json"
      provides: "Config TypeDoc na raiz — entryPoints src/index.ts, excludeProtected, out docs/api, docsRoot ./docs, plugin markdown+theme"
      contains: "excludeProtected"
    - path: "docs/.vitepress/config.mts"
      provides: "Config VitePress — base, nav/sidebar Diátaxis, local search, import do typedoc-sidebar.json"
      contains: "base"
    - path: "docs/index.md"
      provides: "Home/landing (VitePress home layout)"
    - path: "package.json"
      provides: "Scripts docs:dev/build/preview + predocs:dev/predocs:build e as 4 devDeps de docs"
      contains: "docs:build"
  key_links:
    - from: "package.json (predocs:build)"
      to: "typedoc.json"
      via: "hook npm pre* roda typedoc ANTES do vitepress build — gera docs/api antes do config.mts importar o sidebar"
      pattern: "predocs:build"
    - from: "docs/.vitepress/config.mts"
      to: "docs/api/typedoc-sidebar.json"
      via: "import estático do sidebar gerado, injetado em themeConfig.sidebar['/api/']"
      pattern: "typedoc-sidebar"
    - from: "typedoc.json"
      to: "docs/api (Reference gerada)"
      via: "excludeProtected: true impede vazamento dos campos protected Symbol-keyed do Database (D-04)"
      pattern: "excludeProtected"
---

<objective>
Montar o stack de documentação greenfield — o "walking skeleton" de docs: instalar as 4 devDeps (VitePress + TypeDoc + plugin markdown + vitepress-theme), criar `typedoc.json` (raiz) e `docs/.vitepress/config.mts`, adicionar os scripts npm, gerar a Reference de API real de `src/index.ts` e scaffoldar a árvore Diátaxis com uma Home real + páginas-stub para os 4 quadrantes e a migração — tudo buildando verde com `npm run docs:build`.

Purpose: Estabelecer a fundação buildável do site (DOCS-01 infra + DOCS-02 Reference) para que os planos de conteúdo (Wave 2) apenas preencham páginas já existentes, sem tocar em `config.mts`/`typedoc.json` — habilitando execução paralela sem conflito de arquivos.
Output: Site VitePress que builda verde, com Home + Reference integrada (TypeDoc via typedoc-plugin-markdown, sem internos vazados) + esqueleto navegável dos 4 quadrantes + página de migração (stub).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-site-de-documenta-o/04-CONTEXT.md
@.planning/phases/04-site-de-documenta-o/04-RESEARCH.md
@.planning/phases/04-site-de-documenta-o/04-VALIDATION.md

# Superfície pública exata a documentar (Reference) e config atual
@src/index.ts
@package.json
@tsconfig.json
@.gitignore
</context>

## Artifacts this phase produces (escopo deste plano)

- `typedoc.json` (raiz) — config TypeDoc
- `docs/.vitepress/config.mts` — config VitePress (nav/sidebar completos dos 4 quadrantes + Home + Migration + Reference)
- `docs/index.md` — Home/landing real
- `docs/api/**` — Reference GERADA pelo TypeDoc (git-ignorada; regenerada por `predocs`)
- Scripts `docs:dev`/`docs:build`/`docs:preview` + `predocs:dev`/`predocs:build` no `package.json`
- devDeps: `vitepress`, `typedoc@^0.28.19`, `typedoc-plugin-markdown`, `typedoc-vitepress-theme`
- Entrada `docs/api/` no `.gitignore`
- Páginas-stub (preenchidas nas Waves 2): `docs/tutorials/getting-started.md`, `docs/how-to/{hooks,sanitize-filters,handle-errors,escape-hatch,indexes-validation}.md`, `docs/explanation/{thin-odm-philosophy,proxy-gating,server-side-validation}.md`, `docs/migration.md`

<tasks>

<task type="auto">
  <name>Task 1: Instalar devDeps de docs + scripts npm + gitignore</name>
  <read_first>
    - package.json (scripts existentes, devDependencies, engines ^20.19.0 || >=22.12.0)
    - .gitignore (padrão de artefatos de build já ignorados: lib/, dist/, coverage)
    - .planning/phases/04-site-de-documenta-o/04-RESEARCH.md §"Standard Stack" (versões confirmadas) e §"Package Legitimacy Audit"
  </read_first>
  <files>package.json, package-lock.json, .gitignore</files>
  <action>
    Instalar as 4 devDeps de documentação numa única passada para o npm resolver o peerDependency cruzado TypeDoc↔plugin (Pitfall 4 do RESEARCH):
    `npm install -D vitepress@^1.6.4 typedoc@^0.28.19 typedoc-plugin-markdown@^4.12.0 typedoc-vitepress-theme@^1.1.3`.

    PINAR `typedoc@^0.28.19` deliberadamente (NÃO 0.28.20): o 0.28.20 foi sinalizado `SUS` pelo heurístico "too-new" (patch de 3 dias) no Package Legitimacy Audit; o 0.28.19 (estável, ~3 meses, mesma faixa suportada pelo plugin@4.12) sai do território "too-new" sem perder compatibilidade — por isso NENHUM checkpoint humano de supply-chain é necessário aqui (ver `<threat_model>` T-04-SC). As outras 3 devDeps foram auditadas `[OK]`; nenhuma tem `postinstall` (verificado vazio no RESEARCH).

    Adicionar ao bloco `scripts` do package.json (Pattern 3 do RESEARCH — os hooks `pre*` garantem que o TypeDoc gera `docs/api` ANTES do VitePress ler o sidebar):
    `"predocs:dev": "typedoc"`, `"predocs:build": "typedoc"`, `"docs:dev": "vitepress dev docs"`, `"docs:build": "vitepress build docs"`, `"docs:preview": "vitepress preview docs"`.

    Adicionar ao `.gitignore` a entrada `docs/api/` (Open Question 1 resolvida: a Reference é artefato de build, regenerada por `predocs` a cada build local e no CI de deploy — análoga a `lib/`, que já é ignorada). Adicionar também `docs/.vitepress/cache/` e `docs/.vitepress/dist/` (cache e saída de build da VitePress).

    NÃO tocar em nenhum arquivo fora dos três listados; `typedoc.json`/`config.mts` são criados na Task 2/3.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const d=p.devDependencies; ['vitepress','typedoc','typedoc-plugin-markdown','typedoc-vitepress-theme'].forEach(k=>{if(!d[k])throw new Error('missing devDep '+k)}); ['docs:dev','docs:build','docs:preview','predocs:dev','predocs:build'].forEach(s=>{if(!p.scripts[s])throw new Error('missing script '+s)}); if(!/^\^?0\.28\.(19|2[0-9]|[3-9][0-9])/.test(d.typedoc)&&!d.typedoc.startsWith('^0.28.19'))console.error('check typedoc pin');"</automated>
  </verify>
  <acceptance_criteria>
    - As 4 devDeps aparecem em `devDependencies`, com `typedoc` pinado em `^0.28.19`.
    - Os 5 scripts (`docs:dev/build/preview`, `predocs:dev/build`) existem em `package.json`.
    - `.gitignore` contém `docs/api/`, `docs/.vitepress/cache/` e `docs/.vitepress/dist/`.
    - `package-lock.json` atualizado e commitado (pins de supply-chain aprovados).
  </acceptance_criteria>
  <done>devDeps instaladas + scripts + gitignore prontos; `npm ls vitepress typedoc typedoc-plugin-markdown typedoc-vitepress-theme` resolve sem UNMET/peer conflict.</done>
</task>

<task type="auto">
  <name>Task 2: typedoc.json na raiz (Reference sem internos vazados)</name>
  <read_first>
    - src/index.ts (barrel raiz — a superfície pública EXATA a cobrir: Database, Model, MongoatError + 3 subclasses, toObjectId, sanitizeFilter, METHODS, CUSTOM_VALIDATION, tipos)
    - src/database/index.ts:28-38 (os campos `protected [kClient]`/`[kDb]`/`[kConnecting]`/`[kConnectionUrl]` Symbol-keyed que TypeDoc inclui por padrão)
    - .planning/phases/04-site-de-documenta-o/04-RESEARCH.md §"Pattern 1" e §"Pitfall 1"
  </read_first>
  <files>typedoc.json</files>
  <action>
    Criar `typedoc.json` na RAIZ do repo (ao lado de tsconfig.json, para os aliases `@/*` resolverem — Pattern 1 do RESEARCH). Chaves obrigatórias:
    `entryPoints: ["src/index.ts"]`, `out: "docs/api"`, `docsRoot: "./docs"` (D-04/Pattern 1 — sem isto a sidebar gerada aponta para caminhos errados porque `docs/` e `src/` vivem em dirs diferentes da mesma raiz), `plugin: ["typedoc-plugin-markdown", "typedoc-vitepress-theme"]`, `excludeProtected: true`, `readme: "none"`, `indexFormat: "table"`.

    `excludeProtected: true` é OBRIGATÓRIO (Pitfall 1 / D-04): TypeDoc só exclui `private` por padrão; os campos Symbol-keyed do `Database` são `protected` e vazariam na Reference pública sem a flag. Cobrir SÓ a API pública exportada do barrel raiz (`src/index.ts`), nada mais.

    Rodar `npm run predocs:build` (executa `typedoc`) e confirmar que gera `docs/api/**` incluindo `docs/api/typedoc-sidebar.json` e uma página de classe para `Database` sem os membros internos.
  </action>
  <verify>
    <automated>npm run predocs:build && test -f docs/api/typedoc-sidebar.json && node -e "const fs=require('fs');const g=require('child_process').execSync('grep -rlE \"kClient|kConnecting|kConnectionUrl\" docs/api || true').toString().trim(); if(g)throw new Error('internos protected vazaram na Reference: '+g);"</automated>
  </verify>
  <acceptance_criteria>
    - `typedoc.json` existe na raiz com `excludeProtected: true`, `entryPoints: ["src/index.ts"]`, `out: "docs/api"`, `docsRoot: "./docs"` e o array `plugin` com os dois plugins.
    - `npm run predocs:build` roda o TypeDoc sem erro e gera `docs/api/typedoc-sidebar.json`.
    - Nenhum arquivo em `docs/api/**` referencia os símbolos internos protected (kClient/kConnecting/kConnectionUrl) — grep vazio.
    - A Reference cobre Database, Model, as 4 classes de erro, toObjectId, sanitizeFilter, METHODS.
  </acceptance_criteria>
  <!-- planner-discipline-allow: kClient -->
  <!-- planner-discipline-allow: kConnecting -->
  <!-- planner-discipline-allow: kConnectionUrl -->
  <done>Reference gerada de src/index.ts, integrada como markdown, com `excludeProtected` fechando o vazamento de internos (DOCS-02).</done>
</task>

<task type="auto">
  <name>Task 3: config.mts + Home + scaffold Diátaxis (build verde)</name>
  <read_first>
    - .planning/phases/04-site-de-documenta-o/04-RESEARCH.md §"Pattern 2" (config.mts do repo de exemplo oficial) e §"Pitfall 2" (base '/mongoat/')
    - .planning/phases/04-site-de-documenta-o/04-CONTEXT.md §D-02 (os 4 quadrantes Diátaxis + Home + Migration)
    - docs/api/typedoc-sidebar.json (gerado na Task 2 — importado pelo config)
  </read_first>
  <files>docs/.vitepress/config.mts, docs/index.md, docs/tutorials/getting-started.md, docs/how-to/hooks.md, docs/how-to/sanitize-filters.md, docs/how-to/handle-errors.md, docs/how-to/escape-hatch.md, docs/how-to/indexes-validation.md, docs/explanation/thin-odm-philosophy.md, docs/explanation/proxy-gating.md, docs/explanation/server-side-validation.md, docs/migration.md</files>
  <action>
    Criar `docs/.vitepress/config.mts` (ESM, extensão `.mts`) baseado no config real do repo de exemplo oficial (Pattern 2). Setar: `title: 'Mongoat'`, `description: 'A lightweight, type-safe MongoDB ODM'`, `base: '/mongoat/'` (Pitfall 2 — GitHub project page serve em subpath; sem isto o site quebra só após o deploy), `themeConfig.search: { provider: 'local' }` (busca minisearch built-in — precisa ser setada explicitamente), importar o sidebar gerado via `import typedocSidebar from '../api/typedoc-sidebar.json'`.

    `nav` (D-02 — 4 quadrantes + Home + Migration): Home `/`, Tutorials `/tutorials/getting-started`, How-to `/how-to/hooks`, Reference `/api/`, Explanation `/explanation/thin-odm-philosophy`, Migration `/migration`.

    `sidebar` por seção (as páginas de conteúdo são preenchidas nas Waves 2, mas os STUBS existem já nesta task para o build ficar verde):
    - `/tutorials/`: itens → { 'Getting started' → /tutorials/getting-started }
    - `/how-to/`: itens → Register pre/post hooks (/how-to/hooks), Sanitize untrusted filters (/how-to/sanitize-filters), Handle errors (/how-to/handle-errors), Use the native escape hatch (/how-to/escape-hatch), Define indexes & validation (/how-to/indexes-validation)
    - `/explanation/`: itens → The thin ODM philosophy (/explanation/thin-odm-philosophy), Why Proxy gating (/explanation/proxy-gating), Server-side validation (/explanation/server-side-validation)
    - `/api/`: { text: 'Reference', items: typedocSidebar }

    Criar `docs/index.md` como Home REAL usando o VitePress home layout (front-matter `layout: home` com `hero` — name Mongoat, tagline curta, actions "Get started" → /tutorials/getting-started e "API Reference" → /api/ — e `features` em bullets: thin ODM sobre o driver oficial, hooks pre/post, validação server-side $jsonSchema, segurança injection-safe, escape hatch nativo, type-safe). Idioma inglês (Claude's Discretion — consistência com README/CHANGELOG/MIGRATION).

    Criar os 10 arquivos-stub de conteúdo (serão preenchidos nas Waves 2), cada um com apenas um H1 e uma linha de placeholder em inglês, para o `docs:build` não quebrar por página ausente/link morto:
    - docs/tutorials/getting-started.md (`# Getting started`)
    - docs/how-to/hooks.md, sanitize-filters.md, handle-errors.md, escape-hatch.md, indexes-validation.md
    - docs/explanation/thin-odm-philosophy.md, proxy-gating.md, server-side-validation.md
    - docs/migration.md (`# Migration guide — alpha → v1.0`)

    NÃO escrever conteúdo real dos guias aqui (é escopo das Waves 2) — apenas os stubs mínimos. NÃO usar links markdown internos apontando para páginas ainda inexistentes dentro dos stubs (evita dead-link no build).
  </action>
  <verify>
    <automated>npm run docs:build && test -d docs/.vitepress/dist</automated>
  </verify>
  <acceptance_criteria>
    - `docs/.vitepress/config.mts` existe com `base: '/mongoat/'`, `search: { provider: 'local' }`, a nav dos 4 quadrantes + Home + Migration, e o import de `../api/typedoc-sidebar.json` injetado em `sidebar['/api/']`.
    - `docs/index.md` renderiza como Home layout (hero + features), não como stub.
    - Os 10 stubs existem nos caminhos exatos da nav/sidebar.
    - `npm run docs:build` completa verde (predocs roda typedoc, depois vitepress build) e gera `docs/.vitepress/dist`.
    - Nenhum dead-link reportado pelo VitePress no build.
  </acceptance_criteria>
  <done>Site builda verde com Home real + Reference integrada + esqueleto navegável dos 4 quadrantes + migração (stub) — pronto para os planos de conteúdo da Wave 2 preencherem sem tocar config.mts/typedoc.json (DOCS-01 infra).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → devDependencies | Código de terceiros (4 novas devDeps) executa no build local e no runner de CI |
| src/index.ts → Reference pública | O que a TypeDoc extrai vira conteúdo público — internos podem vazar |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | 4 novas devDeps (vitepress/typedoc/plugin/theme) | medium | mitigate | Package Legitimacy Audit já rodado (RESEARCH): 3 pacotes `[OK]`, `typedoc` `SUS` só por "too-new"; mitigado pinando `typedoc@^0.28.19` (versão estável, não-flagged); instalar os 4 juntos; nenhum `postinstall` (verificado vazio); commitar `package-lock.json` |
| T-04-02 | Information Disclosure | Reference gerada (docs/api) | low | mitigate | `excludeProtected: true` no typedoc.json — impede os campos protected Symbol-keyed do Database vazarem na Reference pública (D-04); verificado por grep em docs/api |
| T-04-SC | Tampering | npm installs | medium | mitigate | Ver T-04-01. Como a versão instalada de `typedoc` (`^0.28.19`) NÃO é a flagged `SUS`, e as outras 3 são `[OK]`, não há pacote `[ASSUMED]`/`[SUS]` sendo instalado — nenhum checkpoint humano de supply-chain é exigido (protocolo do planner: pin da versão não-flagged como mitigação alternativa aprovada no RESEARCH) |
</threat_model>

<verification>
- `npm run docs:build` verde (predocs typedoc + vitepress build, sem dead-links).
- Reference gerada de src/index.ts sem internos protected (grep vazio em docs/api).
- Home renderiza como landing (hero + features); nav dos 4 quadrantes + Home + Migration + Reference presente.
- `package-lock.json` atualizado; `npm ls` sem peer conflict.
</verification>

<success_criteria>
- Stack de docs instalado e buildando verde localmente.
- DOCS-02 (Reference TypeDoc integrada, só o barrel, sem vazamento) satisfeito e verificável por build+grep.
- Esqueleto Diátaxis navegável pronto para preenchimento paralelo na Wave 2.
</success_criteria>

<output>
Create `.planning/phases/04-site-de-documenta-o/04-01-SUMMARY.md` when done
</output>
