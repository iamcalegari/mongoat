---
phase: 04-site-de-documenta-o
plan: 06
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05"]
files_modified:
  - .github/workflows/docs.yml
  - README.md
  - package.json
autonomous: false
requirements: [DOCS-01, DOCS-04]
user_setup:
  - service: github-pages
    why: "Publicar o site estático (D-01) — o Pages precisa ter Source = GitHub Actions habilitado (passo de UI do repo, sem CLI/API confiável)"
    dashboard_config:
      - task: "Habilitar GitHub Pages com Source: GitHub Actions"
        location: "GitHub → repo Settings → Pages → Build and deployment → Source: GitHub Actions"
must_haves:
  truths:
    - "Um push na main dispara .github/workflows/docs.yml que builda e faz deploy do site no GitHub Pages"
    - "O README tem um quick start funcional que aponta para o site e NÃO contém o disclaimer work-in-progress"
    - "package.json tem homepage apontando para o site publicado"
    - "O site fica acessível em https://iamcalegari.github.io/mongoat/"
  artifacts:
    - path: ".github/workflows/docs.yml"
      provides: "Workflow de deploy Pages (separado do ci.yml) — build + upload + deploy"
      contains: "deploy-pages"
    - path: "README.md"
      provides: "README renovado (quick start + badges + features + link para o site, sem WIP)"
    - path: "package.json"
      provides: "campo homepage → site"
      contains: "homepage"
  key_links:
    - from: ".github/workflows/docs.yml"
      to: "GitHub Pages"
      via: "permissions minimais (contents:read, pages:write, id-token:write) + actions/deploy-pages, trigger push main"
      pattern: "id-token"
    - from: "README.md"
      to: "https://iamcalegari.github.io/mongoat/"
      via: "seção Full documentation → site"
      pattern: "iamcalegari.github.io/mongoat"
    - from: "package.json (homepage)"
      to: "site publicado"
      via: "campo homepage do npm"
      pattern: "homepage"
---

<objective>
Fechar a fase publicando o site e renovando o README: criar o workflow de deploy `.github/workflows/docs.yml` (GitHub Pages via Actions, SEPARADO do `ci.yml`), renovar o `README.md` (quick start funcional + badges + features + "Full documentation → site", sem o disclaimer work-in-progress) e adicionar `homepage` ao `package.json`. Inclui checkpoints para habilitar o Pages no repo e confirmar o deploy verde.

Purpose: Entregar DOCS-01 (site PUBLICADO) e DOCS-04 (README renovado) — a capacidade final da fase: o site está no ar e o README aponta para ele.
Output: workflow de deploy funcionando, site acessível na URL Pages, README renovado, homepage no package.json.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-site-de-documenta-o/04-CONTEXT.md
@.planning/phases/04-site-de-documenta-o/04-RESEARCH.md

# Padrões existentes e o README a renovar
@.github/workflows/ci.yml
@README.md
@package.json
@examples/model/model.ts
@examples/model/usage.ts
</context>

## Artifacts this phase produces (escopo deste plano)

- `.github/workflows/docs.yml` — deploy Pages (separado do ci.yml)
- `README.md` renovado — quick start + link para o site, sem WIP
- `package.json` — campo `homepage`

<!-- planner-discipline-allow: work in progress -->

<tasks>

<task type="auto">
  <name>Task 1: Workflow de deploy docs.yml (GitHub Pages via Actions)</name>
  <read_first>
    - .github/workflows/ci.yml (padrão existente: actions/checkout@v5, setup-node, cache npm, matriz — o deploy NÃO deve tocar este arquivo)
    - .planning/phases/04-site-de-documenta-o/04-RESEARCH.md §"Supporting" (versões das actions) e §"Pitfall 5" (workflow separado do CI) e §"Security Domain"
    - package.json (script docs:build criado no Plano 01)
  </read_first>
  <files>.github/workflows/docs.yml</files>
  <action>
    Criar `.github/workflows/docs.yml` (NOVO, totalmente separado do `ci.yml` — Pitfall 5/D-01) usando o método oficial "Actions" (não branch gh-pages). Estrutura (baseada no guia oficial vitepress.dev/guide/deploy + repo de exemplo):
    - `on: push: branches: [main]` + `workflow_dispatch`.
    - `permissions:` MINIMAIS — `contents: read`, `pages: write`, `id-token: write` (escopo mínimo; nenhum secret custom/PAT — só o `GITHUB_TOKEN` automático; ver `<threat_model>` T-04-03/T-04-04).
    - `concurrency: { group: 'pages', cancel-in-progress: false }`.
    - Job `build`: `actions/checkout@v5`, `actions/setup-node@v4` (node 22.x, cache npm — coerente com ci.yml, A2 do RESEARCH), `npm ci`, `npm run docs:build` (o `predocs:build` roda o typedoc antes automaticamente), `actions/configure-pages@v4`, `actions/upload-pages-artifact@v3` com `path: docs/.vitepress/dist`.
    - Job `deploy`: `needs: build`, `environment: { name: github-pages, url: ${{ steps.deployment.outputs.page_url }} }`, `actions/deploy-pages@v4` (id: deployment).

    Confirmar que o YAML é válido e que NÃO edita `ci.yml`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const y=fs.readFileSync('.github/workflows/docs.yml','utf8');['pages: write','id-token: write','deploy-pages','upload-pages-artifact','docs:build','branches: [main]'].forEach(t=>{if(!y.includes(t))throw new Error('docs.yml faltando: '+t)}); if(y.includes('secrets.') && !y.includes('GITHUB_TOKEN'))throw new Error('secret custom inesperado');" && test -f .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/docs.yml` existe com trigger `push` na `main`, `permissions` minimais (contents:read, pages:write, id-token:write), jobs build+deploy e as actions configure/upload/deploy-pages.
    - Nenhum secret custom/PAT — só o `GITHUB_TOKEN` automático.
    - `ci.yml` intacto (deploy separado do CI de testes).
    - YAML válido.
  </acceptance_criteria>
  <done>Workflow de deploy Pages criado, separado do ci.yml, com permissions minimais (DOCS-01 deploy).</done>
</task>

<task type="auto">
  <name>Task 2: README renovado + homepage no package.json</name>
  <read_first>
    - README.md (o work-in-progress atual — linha 14 tem o disclaimer a remover; Quick Start desatualizado)
    - examples/model/model.ts + examples/model/usage.ts (quick start funcional real a espelhar)
    - .planning/phases/04-site-de-documenta-o/04-CONTEXT.md §D-03 (README enxuto + link pro site; fonte da verdade é o site)
    - package.json (onde adicionar homepage)
  </read_first>
  <files>README.md, package.json</files>
  <action>
    Renovar o `README.md` conforme D-03 (enxuto, não duplica os guias — a fonte da verdade é o site):
    - REMOVER o disclaimer work-in-progress (o blockquote `### _🚧 This documentation is a work in progress...`) por completo.
    - Manter/ajustar badges (o badge shields.io de versão npm já é dinâmico) e o cabeçalho/logo.
    - Features em bullets (thin ODM sobre o driver oficial, hooks pre/post, validação server-side $jsonSchema, segurança injection-safe, escape hatch nativo, type-safe, dual CJS/ESM).
    - Quick Start FUNCIONAL espelhando `examples/model/model.ts` + `examples/model/usage.ts` (connect → `new Model` com schema → CRUD), refletindo a API v1.0 (assinatura `ctx` nos hooks, `new Model(...)`). Substituir o Quick Start antigo desatualizado.
    - Seção "**Full documentation → https://iamcalegari.github.io/mongoat/**" apontando para o site (com links diretos para Tutorials/Reference/Migration).
    - Manter Installation, Contributing, License.

    Adicionar `"homepage": "https://iamcalegari.github.io/mongoat/"` ao `package.json` (D-01 — homepage apontando para o site; aparece no npmjs.com).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const r=fs.readFileSync('README.md','utf8').toLowerCase(); if(r.includes('work in progress'))throw new Error('disclaimer WIP ainda presente'); if(!fs.readFileSync('README.md','utf8').includes('iamcalegari.github.io/mongoat'))throw new Error('link do site ausente'); const p=require('./package.json'); if(!p.homepage||!p.homepage.includes('iamcalegari.github.io/mongoat'))throw new Error('homepage ausente');"</automated>
    <human-check>Ler o README renovado: quick start funcional (compila contra a API v1.0), sem WIP, com link claro para o site.</human-check>
  </verify>
  <acceptance_criteria>
    - README NÃO contém mais "work in progress" (grep case-insensitive == 0).
    - README tem quick start funcional espelhando os `examples/` (API v1.0) + seção "Full documentation → site".
    - `package.json` tem `homepage` apontando para a URL Pages.
    - Badges/logo preservados.
  </acceptance_criteria>
  <done>README renovado (sem WIP, quick start funcional, link para o site) + homepage no package.json (DOCS-04).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Habilitar GitHub Pages e confirmar deploy verde</name>
  <action>
    O que já está pronto: o workflow `.github/workflows/docs.yml` (deploy Pages via Actions) e o site buildando localmente verde (`npm run docs:build`). O deploy real precisa que o GitHub Pages do repo esteja com Source = GitHub Actions — passo de UI do repositório, sem CLI/API confiável para automatizar (RESEARCH §"Environment Availability").

    Passos manuais para o humano:
    1. No GitHub, ir em repo Settings → Pages → Build and deployment e setar Source: GitHub Actions (se ainda não estiver).
    2. Fazer merge/push desta fase na `main` (ou disparar `workflow_dispatch` do workflow "docs" na aba Actions).
    3. Aba Actions: confirmar que o workflow "docs" roda verde (jobs build e deploy).
    4. Abrir https://iamcalegari.github.io/mongoat/ e confirmar: a Home carrega (CSS/assets OK — valida o `base: '/mongoat/'`), a nav dos 4 quadrantes + Reference + Migration funciona, e a busca local responde.
  </action>
  <verify>
    <human-check>Run do docs.yml verde na aba Actions e o site acessível na URL Pages, com assets, nav e busca funcionando.</human-check>
  </verify>
  <done>GitHub Pages habilitado (Source: Actions), workflow "docs" verde, site no ar em https://iamcalegari.github.io/mongoat/ (DOCS-01 publicado).</done>
  <resume-signal>Digite "approved" quando o run do docs.yml estiver verde e o site acessível na URL Pages; ou descreva o erro (ex.: assets 404 → revisar `base`).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → GitHub Pages | O workflow publica artefato no Pages usando o token automático |
| package.json/README → npm registry + GitHub | Metadata pública renderizada por npmjs.com/GitHub |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Elevation of Privilege | Permissions do workflow docs.yml | medium | mitigate | `permissions` restritas ao mínimo (contents:read, pages:write, id-token:write); usar só o `GITHUB_TOKEN` automático, nunca PAT/secret custom; `concurrency` guard para não sobrepor deploys |
| T-04-04 | Information Disclosure | Logs do workflow de deploy | low | mitigate | Nenhum secret custom no workflow → nada sensível para vazar em log; o `id-token` OIDC é escopado ao deploy do Pages |
| T-04-12 | Tampering | Trigger de deploy | low | accept | Deploy só em `push` na `main` (branch protegida pelo fluxo de PR) + `workflow_dispatch` manual; não há deploy a partir de forks/PRs não confiáveis |
</threat_model>

<verification>
- `docs.yml` válido, permissions minimais, sem secret custom, ci.yml intacto.
- README sem "work in progress", com link do site; homepage no package.json (grep automatizado).
- Checkpoint: run do docs.yml verde + site acessível em https://iamcalegari.github.io/mongoat/ com assets/nav/busca OK.
</verification>

<success_criteria>
- DOCS-01 (site PUBLICADO) e DOCS-04 (README renovado) satisfeitos.
- Deploy automático na main funcionando; site no ar na URL Pages.
</success_criteria>

<output>
Create `.planning/phases/04-site-de-documenta-o/04-06-SUMMARY.md` when done
</output>
